{
  "name": "danbridge-scheduler-v11",
  "private": true,
  "scripts": {
    "test": "node tools/audit_full_system.js && node tools/audit_scenarios.js && node tools/audit_calendar_scenarios.js && node --test tests/cloud-sharded-store.test.mjs tests/sharded-sync-stress.test.mjs",
    "test:e2e": "playwright test",
    "test:rules": "node tools/run_firestore_rules_tests.mjs",
    "deploy:production": "firebase deploy --only hosting --project production",
    "deploy:staging": "firebase deploy --only hosting,firestore:rules --project staging"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@playwright/test": "1.55.1",
    "firebase": "^12.17.1"
  }
}
