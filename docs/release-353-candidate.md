# 20.26.358 — leave completion, shared book requests, and UI hardening (released; continued from 353)

Date: 2026-09-19 (Asia/Taipei). Released to production after the documented gates below passed.

## Implemented

- Dashboard leave badge counts pending requests and decisions still awaiting explicit completion. Approve, reject, cancel, opening the page, and changing filters do not complete a new request. Only an Owner's separately confirmed Complete transaction clears that item. Completion preserves the decision, hours, dates and payroll facts. Previously decided legacy records are not retroactively reopened.
- New independent Book Requests page for authorized Owner, teacher/scheduler and branch-manager accounts. Shared reading; exact creator UID and email are required for updates and deletion, including when the actor is an Owner. No student/salary/finance information is added to book notifications.
- Local UI updates without awaiting cloud writes. Durable per-operation local-storage entries, ordered background submission, stable operation IDs and server receipts prevent duplicate replay. Per-operation keys protect unrelated pending work in another tab of the same account. Errors preserve drafts; corrupt legacy drafts are not overwritten.
- Authenticated existing notification stream triggers fresh book reads; 30-second visible-page polling is a fallback, not the normal update path. Read generations and identity checks fence stale/account-switched responses.
- Centered controls now follow the shared 48px system: 15px desktop / 16px compact form text and 14px action text. Circular red counts grow with digit count. No change to Daniel's Owner authorization, schedule authority, finance/payroll logic or Firestore rules.

## Evidence completed

- Final browser suite: **72 passed**, 8 desktop/tablet/mobile Chromium/WebKit configurations. Includes real clicks against isolated test backends: repeated approve/reject/cancel/complete, failed writes, 0–1,000 badge counts, cross-role navigation, creator-only controls, 20 additions with cloud intentionally held, retry, corrupt drafts, two-tab durable-queue preservation and explicit version-conflict recovery.
- In the 20-addition isolated browser cases, all measured submit-to-two-animation-frame samples satisfied the test bound of **250ms**. This is not a claim of sustained 120Hz or a measurement of real cloud delivery.
- Native Firestore emulator: **13 passed, 0 skipped**. Real transactions, concurrent duplicate submissions, revoked access, four-recipient scoped notification reads/acknowledgments, creator guards, and separate leave approval/completion preserving leave facts. Accounts in emulator are fixtures, not four actual Google logins.
- Final npm regression: **1,563 passed, 0 failed, 11 skipped** (additional native tests are intentionally skipped outside the emulator). Emulator-specific skipped cases must not be described as production acceptance. A subsequent UI-only explicit conflict-recovery refinement has its own browser check.
- Safari staging: Daniel signed in as Owner, updated page shows Book Requests and successfully reports its initial cloud read as synchronized.
- Production root re-read on this turn still reports **20.26.351**. No production deploy, production test writes or GitHub push this turn.

## Staging state and remaining acceptance

The staging leave-operation function and new staging book-purchase function were deployed successfully. The final 353 hosting candidate was deployed. Remote SHA-256/readback of the book module, Firebase integration module and new stylesheet matches local files exactly.

Safari Daniel page has the unsent synthetic title `STAGING_BOOK_353_ACCEPTANCE`, quantity 1. It was preserved. Safari private browsing was unlocked, and AA completed the separate Google passkey authentication. AA is visibly signed in as branch manager; the Book Requests page and successful initial cloud list read were observed in both Chinese and English. Initial role-data confirmation exceeded 20 seconds before recovering; do not count that as fast-start acceptance. AA now has the unsent title `STAGING_BOOK_353_AA_ACCEPTANCE`, quantity 1, awaiting specific submission confirmation.

Read-only staging access-document verification confirms all four authorized accounts are active and in the correct company: Daniel/Catherine Owner, AA branch manager (one branch and linked teacher), 張毅 teacher. No access documents were modified. This is not a substitute for four authenticated browser write tests.

Not yet verified:

1. Four actual Google-account create/update/delete and receiving-page checks for the new books feature, including real Auth/App Check callable delivery and notification acknowledgment.
2. Actual cloud synchronization latency versus the existing schedule baseline; no speed equivalence claim is justified yet.
3. Real leave Complete roundtrip and second-device badge disappearance on the final hosted candidate.
4. Same-book edits fail closed on revision conflict. A local recovery button now archives the queued draft, fetches the latest revision, and restores the draft to the form for an explicit new submission (never silently overwriting newer cloud data). This flow still needs the final hosted/live-device check.
5. Final exact candidate promotion, production post-deploy checks and GitHub commit/push.

Do not replace the above remaining checks with emulator or mock-browser results.

## Live follow-up — AA submission authorized

- Safari AA submitted `STAGING_BOOK_353_AA_ACCEPTANCE`, quantity 1. It appeared immediately as pending, then synchronized. Read-only Firestore verification found exactly one matching record (`book-df5fe655-000a-4dbc-b925-e97538857c9b`), revision 1, correct AA creator, and one operation receipt. No precise cloud latency was measured.
- All four exact recipients have a matching book notification document. Daniel and Catherine actual Safari pages displayed AA's record as View Only, without edit/delete controls. Daniel's acknowledgment succeeded.
- Catherine's first book acknowledgment failed. Safari Console showed a recaptcha PAT 401 and, separately, a disconnected request to the App Check Enterprise token-exchange endpoint; the thrown error was `appCheck/fetch-network-error`. Do not conflate the PAT status with an established application authorization root cause. The notice stayed unread and retained its recovery UI. One manual retry succeeded; read-back confirmed Catherine's `read: true`.
- AA acknowledgment also succeeded. Final read-only check confirmed Daniel/Catherine/AA `read: true`, 張毅 `read: false` at this checkpoint.
- 張毅 login is now at the Google passkey Touch ID prompt in Safari; user biometric action is required. Existing three sessions and unsent Daniel form are preserved. Teacher actual receiving-page check is still outstanding.
- This one staging book fixture remains for cross-account acceptance and eventual cleanup; it is not a student or production order. No production deployment or GitHub push occurred.
- Full `npm test` rerun after live acceptance preparation and the final conflict-recovery code completed with exit 0: 1,563 passed, 0 failed, 11 skipped, from `/tmp/danbridge-353-regression-live-followup.log`. `git diff --check` also passed. This rerun does not close the outstanding authenticated live cases.

## Live follow-up — teacher receiving check

After the user completed Google login, the private staging web.app page showed the teacher role (staging display name `STAGE`). It displayed the AA book notification, then the shared book list with the AA item read-only, no edit/delete controls. Clicking Got It succeeded. Read-only verification of the exact 張毅 recipient document changed from unread to `read: true`; all four recipients are now confirmed read. Chinese/English switching retained the unsent teacher form. Title `STAGING_BOOK_353_TEACHER_ACCEPTANCE`, quantity 1 is prepared but not submitted, pending action-time submission confirmation. This establishes four-account receipt/read acknowledgment of **one** AA-created book, not all four-account create/update/delete cases. Production is unchanged.

## Live follow-up — teacher create and update

- After confirmation, Safari teacher submitted `STAGING_BOOK_353_TEACHER_ACCEPTANCE`, quantity 1. UI immediately showed one pending operation and the new card; subsequently reported synchronized. Exact Firestore read-back found one record `book-03c8f442-d9f4-4b1c-94b0-418c2867728c`, correct teacher creator, revision 1 and one receipt, with all four recipient notifications present.
- After a separate specific confirmation, teacher saved quantity 2. UI showed 2 books and synchronized. Read-back confirmed revision 2, quantity 2 and exactly two receipts; all four update notifications were present. No timing guarantee is inferred from tool duration.
- AA's live page displayed the teacher's original request read-only and acknowledged its notification. Catherine's live page displayed the updated quantity 2 read-only and successfully acknowledged the update notification. Her unsent own fixture `STAGING_BOOK_353_CATHERINE_ACCEPTANCE`, quantity 1, is prepared for the next confirmed submission.
- No production changes. Both staging fixtures remain pending completion of cross-account acceptance and cleanup.

## Live follow-up — role-control defect found and corrected

- Teacher Safari screenshot showed the Owner immediate-approval checkbox. Root cause: privileged form visibility was refreshed only during form clearing (before asynchronous role restoration), while the flex CSS could override the hidden attribute.
- `syncSelectors` now reapplies current-role visibility and disables/unchecks immediate approval for non-Owners without clearing other form input. Scoped `[hidden]` styling enforces actual invisibility. Server policy already gated immediate approval on the authoritative Owner role; an additional forged-flag unit test covers teacher and scheduler actors.
- New role-switch visual checks passed in all eight browser/device configurations; the complete book/leave/badge browser run passed **80/80**. One initial broader test run could not launch browser subprocesses due to the local sandbox; the approved browser-test invocation then ran successfully. An initial unit run caught the cache-name format assertion; the cache revision was corrected to numeric `374`, and the complete unit regression is rerunning.
- Teacher-book update notifications were read back as `read: true` for all four exact recipients after actual Safari acknowledgments. The original create notification remains unread for some recipients; do not equate this with every historical notification being cleared.
- Teacher list filtering (My Requests, no-match search, clear search) was exercised without changing stored book data. Catherine's create and teacher's delete are awaiting their action-time confirmations. No production release occurred.
- Corrected cache-revision full regression finished: **1,563 passed, 0 failed, 11 skipped**; the separately invoked leave-policy suite passed **9/9**, including forged immediate-approval flags. `git diff --check` passed.
- The Owner-control correction is deployed to staging only. Remote byte comparison matched all three changed assets: teacher-leave.js SHA-256 `e09cd85d7793c2f0a36d6ff942c81be0afc147ef7818eada2849feba4674c4d8`, book/leave CSS `4d64b3914afeb8e142d82dc04f458817f22168cb5c7021c0222436b1c39ffbee`, sw.js `4f0503d2f1d214da0f6609d74a33350d8e7f74a1000306b959979eaa2db4ba55`.
- Teacher's empty form was reloaded, then Update Now accepted. Real auth restored without another Google login, but role-cloud readiness exceeded 20 seconds and entered safe read-only retry. Final hosted visual verification awaits that readiness; this is not fast-start acceptance.

## 354/355 follow-up — actual teacher UI corrected, release still gated

- After a uniform asset-fingerprint update, actual Safari teacher view no longer exposes the Owner immediate-approval checkbox. Teacher selection remains disabled. The new hosted page retained the teacher's exact book record at quantity 2, revision 2; refresh did not duplicate it. Read-only verification again confirmed all four **update** notifications read, distinguished from older create notifications.
- Actual Safari at compact width revealed low-contrast teacher navigation. Scoped light-surface/dark-text styling now fixes that layout, without changing Wendy's orange palette or permission logic. The final Safari screenshot after accepting Update Now shows readable Chinese navigation and the Owner-only control absent. No unsent business form was refreshed.
- Expanded regression initially found three old test expectations per platform that omitted the new Books navigation and Leave dashboard shortcut. Expectations were updated to include these user-requested features, and the render-deferral check now also covers Books and Leave renderers; no authorization or timing assertions were removed.
- Final 355 browser run: **393 passed, 0 failed, 7 skipped** across eight browser/device configurations. The 7 skips are the existing iPad-specific install-guidance case on non-iPad-WebKit projects. Separate compact-navigation Chinese/English contrast tests passed **24/24** over three repetitions. Earlier contrast failures during a CSS transition were fixed by making the compact palette switch immediate.
- Final 355 full npm regression: **1,563 passed, 0 failed, 11 skipped**, exit 0 (`/tmp/danbridge-355-regression.log`). Native Firestore run: **13 passed, 0 failed, 0 skipped**; emulator stopped cleanly. These are automated tests, not additional authenticated four-account UI writes.
- Hosting **355** deployed to **staging only**. Exact remote bytes match local index.html (`68c7bfc0e59a4b412e8d82cddaf92a8369e471e73665b533ba6f66c1c83f87e8`), bilingual CSS (`04f678fa5cb2d3a3a3f8e4f31f5aaa28f76b37ca0909d2f39b5ed7bde4539543`), teacher-leave.js (`e09cd85d7793c2f0a36d6ff942c81be0afc147ef7818eada2849feba4674c4d8`), and sw.js (`0bc1e9baaaed037ff68903a73a1b85b8c8e607c812471b65dbd9d3ae573b4fdb`).
- **Still pending:** action-time confirmation for Catherine's prepared one-book staging creation and the teacher's exact two-book staging fixture deletion. Neither was submitted/deleted. Remaining real four-account own CRUD, live separate leave Complete/badge propagation, live conflict recovery, and measured cloud latency are not closed by these automated results. Both existing book fixtures are retained for acceptance, not production orders.
- No production deployment, production data writes, Git commit or GitHub push in this continuation. Production remains at the previously read version 20.26.351; no universal 100% success claim.

## Live follow-up — confirmed creator operations and Daniel four-recipient update

- Following specific confirmations, teacher deleted `book-03c8f442-d9f4-4b1c-94b0-418c2867728c`. Read-back confirmed revision 3, `deleted: true`, and three operation receipts. The live lists no longer show the item; audit/tombstone data remain. Catherine, Daniel and AA actually received the deletion notification. This was staging test data only.
- Catherine created `STAGING_BOOK_353_CATHERINE_ACCEPTANCE` and then, after a separate confirmation, updated quantity to 2. Read-back found exactly one record `book-ff25a03d-e6a6-42c8-9337-3919ae330ccf`, revision 2 and two receipts. Daniel, AA and teacher actual pages display it read-only. Catherine/teacher update acknowledgments remain outstanding at this checkpoint.
- Daniel created `STAGING_BOOK_353_ACCEPTANCE` and, after the explicit “確認 Daniel 儲存兩本” approval, updated that same record to quantity 2. Read-back found exactly one record `book-d20474c8-fc9b-4ce3-b2cb-2cc8b0480c51`, revision 2 and two receipts. No duplicate creation was made while resuming verification.
- Actual Safari Daniel/Catherine/AA/張毅 pages all displayed Daniel's quantity 2. Catherine, AA and teacher had View Only, without edit/delete controls for Daniel's item. Each account acknowledged the revision-2 update through its own UI; final read-only verification confirmed **all four exact revision-2 notifications `read: true`**. This verifies this update, not every historical notification or all feature cases.
- AA's own quantity-2 form is prepared but **not submitted**, awaiting its specific action-time confirmation. Remaining own-record deletion/cleanup, live leave Complete propagation, conflict recovery and measured cloud latency remain release gates. An English-mode presentation issue was observed: book summary/status/search labels still contain Chinese; this needs a targeted bilingual follow-up.
- Production remains unchanged; no production deployment, permission changes, Git commit or push in this follow-up.

## Continued verification — bilingual correction and remaining read acknowledgments

- Catherine's exact create (revision 1) and update (revision 2) notifications now read back as acknowledged by **all four actual recipients**, after Safari acknowledgments. Teacher's delete (revision 3) notifications likewise read back as acknowledged by all four. These are separate operation/revision checks, not aggregate unread-count assumptions.
- The local language dictionary now covers book search/scope accessibility labels, sync state, request/book counts (including zero and singular), and the conflict-recovery label. No callable, authorization, payroll or queue behavior was modified for this correction. This language change is **local only**, not yet deployed to staging or production.
- Added repeated Chinese/English switching with a held write and an unsent draft: translated UI counts/status must not change the original Chinese title, clear the draft, or add a duplicate operation. The first run found a test addressing a nonexistent placeholder instead of the existing search `aria-label`; that assertion was corrected to the actual attribute, without removing coverage.
- Corrected browser run: **72 passed, 0 failed**, eight Chromium/WebKit desktop/tablet/mobile configurations, including the new language case and existing book optimistic CRUD, creator isolation, retry, conflict and two-tab queue cases. Complete npm regression also passed: **1,563 passed, 0 failed, 11 skipped**, exit 0 (`/tmp/danbridge-355-language-regression.log`). `git diff --check` passed.
- Actual teacher Leave page was inspected: teacher selectors disabled and restricted to the linked synthetic teacher; Owner immediate approval absent; the existing cancelled synthetic leave was read-only. No new leave submission, approval or Complete operation occurred; that live sequence remains outstanding.
- AA's prepared edit remains unsent pending its action-time confirmation; no broad authorization was substituted for that UI submission confirmation. Production remains unchanged.

## Continued live verification — all four creator sequences and cleanup confirmed

- After the explicit AA quantity-two confirmation, AA updated its own exact fixture to quantity 2. All four actual Safari accounts displayed the update, and each acknowledged its notification. Other creators' entries remained View Only.
- After the user's exact three-deletion confirmation, Daniel, Catherine and AA each deleted their own fixture through their authenticated Safari pages. Each record read back as revision 3, `deleted: true`, with exactly three operation receipts. No production records, student entries or orders were changed. Tombstones and audit receipts remain intentionally retained.
- Final read-only checks for **all four book fixtures**, including the previously deleted teacher fixture, found exactly one record per fixture and all **48 expected notifications** (four creators × three operations × four recipients) existing, matching the fixture and `read: true`. Every account's active book list was empty after cleanup. These are exact operation receipts and recipient checks, not assumptions based on a total unread badge.
- A broader 416-case UI suite then found **28 failures**, all in bilingual presentation, with 381 passing and 7 skipped. Root causes were the new module overriding the shared control font sizes, untranslated leave controls/dashboard text, and English navigation labels clipping on compact screens. This invalidates any statement that the broader UI acceptance had already passed.
- Local fixes use the shared control-size variables, cover Books in the cross-workspace sizing audit, translate the identified leave controls and shorten navigation labels without changing their destinations. An intermediate 120-case rerun passed 117 cases and exposed three remaining compact Leave navigation overflows; the label is now corrected to Leave Center, with the full combined rerun in progress.
- Actual teacher form is prepared, **not submitted**, for the synthetic `STAGING_SHADOW_TEACHER`, personal leave 2026-09-04 09:00–17:00, note `STAGING_LEAVE_355_COMPLETE_ACCEPTANCE`. The live preview shows 8 hours / 1 day and one affected synthetic lesson, 16:00–17:00 / 1 hour. This pending submission has its own action-time confirmation request. It does not establish the approval/Complete or remote badge tests as passed.
- No production deployment, Git commit or GitHub push in this continuation. The new local presentation fixes are not yet a hosted release.

## 356 presentation regression and release preparation

- After fixing the remaining compact navigation label, the combined browser suite passed **481 tests, 0 failed, 7 platform-specific skips**, across eight Chromium/WebKit configurations. This includes Books in the shared workspace audit, the new language/draft test, public entry, leave approval, repeated explicit completion and red-circle geometry up to 1,000.
- Asset imports, release constants and service-worker cache were advanced together to **20.26.356** / `danbridge-v11-staging-leave-376`. The record worker was rebuilt from source. The post-bump Chromium/WebKit entry/worker suite passed **82 tests, 0 failed, 2 platform-specific skips**. No worker or scheduler algorithm was changed by the fingerprint update.
- Full post-bump npm regression completed with exit 0: **1,563 passed, 0 failed, 11 skipped**, `/tmp/danbridge-356-regression.log`. Target-config validation and `git diff --check` passed.
- Production HTML was fetched again and exposed only **20.26.351**. Staging-only hosting publication of 356 has started; remote byte verification remains outstanding at this checkpoint.
- Exact read-only staging lookup confirms there is **no** `STAGING_LEAVE_355_COMPLETE_ACCEPTANCE` record yet. The teacher form is unsent and preserved pending its explicit submission confirmation. The actual approval/Complete roundtrip, cross-device badge result, hosted conflict recovery and measured cloud latency remain unverified; the browser mock results do not replace them.

## 356 hosted verification and actual leave approval

- Staging hosting deployment completed. Immediate index/service-worker read-back initially returned 355; subsequent read-back matched local 356 bytes for both, and the changed CSS, language, auth module and generated worker also matched. Production remains 351 and was not deployed.
- Following the specific create confirmation, the teacher submitted the synthetic leave fixture. Read-only verification found exactly one record `leave-63da342fc94f4418a896fb252ea2d589`, pending revision 1, 2026-09-04 09:00–17:00, 8 hours / 1 day. Expected recipients are both Owners and the affected teacher; AA must not receive another teacher's HR record.
- Following the specific approval confirmation, Daniel clicked Approve once. Cloud read-back confirms approved revision 2, exactly two receipts, unchanged 8 hours / 1 day, `requiresCompletion: true`, and no completion timestamp. Daniel's actual balance displays 13/14. Navigating to Dashboard shows **1 outstanding / 0 awaiting approval**; returning to Leave Center still shows the exact approved fixture and a separate Complete button. Complete has not been clicked at this checkpoint; its action-time confirmation is pending.
- All three expected approval notifications exist and reference the exact fixture. Their read acknowledgments and the cross-account post-completion badge are not yet verified.
- Live notification presentation exposed a misleading generic “Active” label for pending leave. A local presentation-only correction now maps pending, approved, rejected and cancelled statuses explicitly, with an unknown-status fallback and escaping preserved. Six focused presentation tests passed. This correction was made after 356 deployment and is **not yet hosted**; it does not change stored approval status or entitlements.
- Catherine's actual Dashboard also shows 1 outstanding and 0 awaiting approval. The teacher page independently shows 13/14 and “waiting for Owner”, without a Complete control. All six expected create/approve notices (two revisions × three authorized recipients) were acknowledged through their actual Safari accounts and read back as matching and `read: true`. AA has neither notice, as expected for this unrelated teacher record. Completion timestamp remains null; no Complete operation was performed.
- Full regression after the notification-label correction finished with exit 0: **1,564 passed, 0 failed, 11 skipped** (`/tmp/danbridge-356-status-regression.log`). Focused notification/scope/leave-policy/runtime tests separately passed 28 with 1 native-emulator test skipped. `git diff --check` passed. These checks do not close the still-pending live Complete, cleanup, conflict recovery or release gates.

## 357 follow-up — real completion, cancellation and notification labels

- Daniel executed the separately requested Complete action. Exact cloud read-back found revision 3, completion timestamp `2026-09-19T04:00:36.427Z`, three receipts, approved status and unchanged 8 hours / 1 day. Actual Daniel and Catherine dashboards both changed to Tasks Complete without a count badge; the balance remained 13/14. Complete intentionally generates no new schedule notification; the backend source explicitly omits recipients for this action.
- Following the exact cancellation confirmation, Catherine cancelled the same synthetic record. Read-back confirms cancelled revision 4, four receipts, expected cancellation notices for Daniel/Catherine/the affected teacher, and no AA notice. Actual balance returned to 14/14. Cancellation correctly reopened its administrative completion task; the final completion of this cleanup is awaiting its own confirmation. No formal leave or payroll record was touched.
- Release imports and service-worker cache advanced uniformly to 358 / `staging-leave-378`, and the generated worker was rebuilt. One redirected browser-test invocation could not launch browsers in the sandbox (EPERM); the normal approved invocation completed **86 passed, 0 failed, 2 platform-specific skips**. These include public entry, current resource fingerprints, real browser worker operations and repeated leave badge behavior.
- Hosting 357 deployed only to staging. Remote bytes match local index, service worker, notification presentation module, auth module, language module and generated worker. The initial post-bump full regression caught an audit script still expecting 356; that exact release expectation was corrected and full regression is rerunning. Do not report that initial audit run as passed.
- Remaining release gates: final cleanup completion, actual hosted conflict recovery and measured synchronization latency. Production remains unchanged; no production deployment, commit or push in this continuation.
- Final corrected 357 full regression: **1,564 passed, 0 failed, 11 skipped**, exit 0, `/tmp/danbridge-357-regression-final.log`; diff whitespace validation also passed. The skipped emulator cases are not counted as fresh live acceptance.
- All **nine** expected create/approve/cancel notifications (three revisions × Daniel/Catherine/affected teacher) now read back as present, exact-fixture matches and read after actual Safari acknowledgments. AA has no unrelated HR notifications. Teacher's actual balance is restored to 14/14; cancelled revision 4 remains awaiting final administrative Complete confirmation.
- Catherine's empty staging page was refreshed and updated to the new candidate. Its cloud readiness briefly entered the over-20-second safe-read-only state before recovering to the populated Dashboard. This is a startup-latency observation, not evidence of immediate startup. Historical unrelated schedule notices were deferred rather than indiscriminately acknowledged. No unsaved business form or formal data was cleared.

## Final gate follow-up — cleanup completed, App Check hardening

- Catherine completed the cancelled test's administrative task. Exact read-back: cancelled revision 5, five receipts, completion timestamp `2026-09-19T04:16:12.836Z`, unchanged 8 hours / 1 day. Actual UI shows 14/14 and no remaining leave tasks. All nine create/approve/cancel notifications remain read; Complete intentionally emits none. The historical test record is retained for audit, not active leave.
- Release review found the new book callable did not explicitly reject `request.app.alreadyConsumed`, despite enabling token consumption. Added this rejection before database access, for both staging and production entry wrappers. A test evaluates the actual wrapper and checks consumed tokens, unverified email, missing App Check and missing auth. No role, balance or scheduling permission was changed. Staging-only book-function deployment is in progress; production has not been updated.
- Full regression after that guard: **1,565 passed, 0 failed, 11 skipped**, exit 0 (`/tmp/danbridge-357-appcheck-regression.log`). Native Firestore book/leave run: **14 passed, 0 failed, 0 skipped**, clean emulator shutdown. The first emulator attempt lacked Java on PATH; the existing cached Java 21 runtime was then used, with no new installation.
- Read-only Cloud Run request-log query from 2026-09-19T00:00:00Z: stagingBookPurchase 741 HTTP requests, all 200, median 0.091s, p95 0.137s, maximum 2.259s. These include reads and are not write-only samples. Five stagingTeacherLeaveOperation requests were all 200; first create was 3.080s, subsequent approval/completion/cancellation/completion 0.184–0.278s. These are backend HTTP latencies only, not UI paint or complete cross-device delivery; no speed-equivalence claim follows.
- Catherine's new synthetic book `STAGING_BOOK_357_CONFLICT_ACCEPTANCE`, quantity 1, is prepared but **not submitted** pending action-time confirmation. Actual same-account dual-tab conflict/recovery remains outstanding. Safari private windows are now locked, so further AA/teacher UI work awaits local unlock (not another Google login). Formal release remains gated as the user explicitly requires every necessary verification before publication.
- Staging book callable update completed successfully. Catherine's actual authenticated Books page then used Retry Sync with an empty queue and remained Synced with zero active requests; the unsent conflict-fixture title was preserved. The strengthened callable's actual write path still awaits the confirmed conflict test. No production function or hosting deployment occurred.

## Hosted conflict acceptance — two real Safari tabs

- Catherine created `STAGING_BOOK_357_CONFLICT_ACCEPTANCE`, ID `book-6bd2db4a-2e4f-486d-82f0-69050fa3a55c`. Read-only cloud verification found quantity 1, revision 1, one receipt and four matching recipient notifications. This creation exercised the newly hardened staging callable; production remains untouched.
- The first Safari tab retains an unsent quantity-3 edit based on revision 1. An unrelated historical schedule notification initially blocked form interaction; View Later dismissed it without acknowledging that historical notification. The quantity-3 field was then visibly verified.
- A new second tab initially reported “Failed to get document because the client is offline.” Retrying the authorized Catherine Google login recovered a populated dashboard and the same book record. This is an observed recoverable startup failure, not a proven root cause or an instant-start pass.
- After the user's specific quantity-2 confirmation, the second tab saved the exact fixture. The first post-click AX observation at 394 ms showed quantity 2 and Pending Sync; the following observation showed Synced. These native-tool observations are not precise browser paint or cloud latency measurements. The first tab still retained quantity 3 and its unsent Save Changes form when revisited. Its stale-write attempt awaits a separate action-time confirmation; conflict rejection/recovery is not yet passed.
- Independent cloud read-back now confirms quantity 2, revision 2, exactly two receipts and four matching update notifications. No stale quantity-3 attempt has been sent.
- Additional native Firestore scheduler regression initially failed because an old test expected an authentication-error message for an authenticated but unauthorized teacher. Corrected only that assertion to the actual authorization error and added a distinct unverified-App-Check test plus a zero-created-lessons check. Rerun passed 1/1, no skips, covering CRUD, replay, concurrent/distributed writers, notification scoping and atomic rollback. No scheduler production implementation changed.
- Native Owner transaction regression additionally passed 6/6, no skips, including both legacy-compatibility modes, 40-lesson atomic changes, roles/notices, replay, revoked access and preparation failures. These ran only against `demo-danbridge-owner-published` on the local emulator; isolated namespaces were cleaned by the test. These timings are not real cloud latency claims. Emulator processes shut down cleanly.
- Production publication remains blocked on the remaining actual stale-write/recovery and final receiving-session checks, not on these now-passing local regressions.
- The confirmed stale revision-1 quantity-3 submission was then attempted through the first actual Safari tab. The UI first rendered the optimistic local card, then the callable rejected it with the exact version-conflict message. Independent cloud read-back remained quantity 2, revision 2, and exactly two receipts: the stale edit did not overwrite the newer cloud record or create a receipt/notification.
- Catherine used the explicit recovery control and accepted its confirmation. After the read completed, the page displayed cloud quantity 2 while the edit form retained quantity 3, with the status `Conflict draft backed up; review the latest data before submitting again`. The conflict outbox intent was archived before removal, and the UI did not silently rebase or auto-submit it. This closes conflict rejection and local recovery; the explicit revision-2 resubmission and cleanup remain pending actions.
- Following the user's explicit continuation confirmation, Catherine resubmitted the retained quantity-3 draft against the refreshed revision 2. The UI showed one pending operation and then Synced. Independent read-back confirms quantity 3, revision 3 and exactly three receipts. Four exact revision-3 recipient notifications exist; Catherine and Daniel acknowledged that exact update through their actual pages. The AA/teacher notification documents exist but their private Safari windows became Touch-ID locked before exact revision-3 UI acknowledgment.
- The full npm regression was rerun after the scheduler-test correction and completed with exit 0. The focused Playwright suite for Books, Leave dashboard badge/approval, bilingual control sizing and public entry also completed with exit 0 across its configured projects. Production target validation, targeted function tests (41 passed, 0 failed, 2 emulator-only skips) and `git diff --check` pass. Native scheduler (1/1), native Owner publication (6/6), and the earlier native Book/Leave run (14/14) separately cover the skipped Firestore cases.
- The exact synthetic record is now at the final browser deletion confirmation. No deletion has occurred yet. Production remains unchanged until cleanup confirmation and the final deployment/read-back gate.

## 357 production release — final cleanup and byte verification

- After the user's explicit deletion confirmation, Catherine deleted the exact staging conflict fixture `book-6bd2db4a-2e4f-486d-82f0-69050fa3a55c` through the authenticated UI. The page returned to Synced with zero active requests. Independent read-back confirms quantity 3, revision 4, `deleted: true`, exactly four receipts and four matching deletion notifications. The tombstone and immutable receipts remain for audit; there is no active staging book request.
- The final full regression completed with exit 0. The focused Playwright Books/Leave/bilingual/public-entry run completed with exit 0. Native Firestore scheduler acceptance passed 1/1, Owner publication passed 6/6, Book/Leave passed 14/14, and the targeted callable/runtime suite passed 41 with two emulator-only skips. Production target validation and `git diff --check` passed.
- Production callable deployment completed: `productionBookPurchase` was created and `productionTeacherLeaveOperation` was updated in `asia-east1`. Firestore rules and the production scheduler callables were not changed by this release.
- Production hosting now serves **20.26.358**. Exact SHA-256 comparisons match the local candidate for `index.html`, `sw.js`, both Leave/Books styles and modules, the authentication/sync module, language module and app shell. The production service worker cache is `danbridge-v11-staging-leave-378`; the historical cache name does not imply a staging backend.
- No staging fixture was copied to production. No production student, schedule, payroll, billing, leave or book record was created or deleted during publication.
