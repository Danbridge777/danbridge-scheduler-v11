# 20.26.309：送出前草稿復原修補

日期：2026-09-12（台北）。以正式 20.26.307／41694c1 為基底，僅移入可獨立驗證的 Owner 草稿復原修補；不切換新後端協定。

## 修正範圍

- 在既有 IndexedDB 中保存送出前的原始基準、最新本機資料、環境、帳號／裝置／分頁範圍及 activation epoch。送出前須確認保存成功。
- 首份雲端快照先復原草稿並依三方合併規則保留衝突，避免備份或規劃失敗、尚未建立操作日誌時，重新整理把未送資料覆蓋。
- 同一筆回條重送後仍合併較新的本機復原動作，不把上一個已提交狀態誤當成最新使用者意圖。
- 保存失敗不得宣稱成功；跨範圍或跨分頁版本不符不得覆蓋。清除後保留版本標記以拒絕舊分頁寫回。
- 舊快取補上預設欄位時，以保存的原始內容核對；不自動刪除無法對應的復原證據。

## 已完成驗證

- 同一修補在隔離 308 的實際 Safari 操作：選取 8 堂，批次延後 30 分鐘，故意於寫入前失敗。雲端維持 revision 4305、124 堂完整雜湊不變。
- 從實際畫面匯出失敗當下資料，重新整理後自動恢復並送出：revision 4321；完整歷史 1919 筆，與匯出資料差異 0。
- 實際將同一組 8 堂提前 30 分鐘還原：revision 4337，全部 124 堂的欄位內容雜湊與測試前相同。四個隔離帳號每人各 8 筆通知、8 個不同課程 ID；AA 分頁實際顯示此通知。正式資料寫入 0。
- 309 獨立正式基底：1／8／20 筆備份失敗、重開、回條遺失、連續復原、草稿額度失敗、跨範圍版本保護測試通過。
- npm test：1148 項，1141 通過、7 跳過、0 失敗。額外修補回歸 77 通過、0 失敗。
- Chromium／WebKit 實際 IndexedDB：6000 筆合成資料、單份內容超過 6 MB，20 筆變更重開保持完整，2 項通過。
- Chromium／WebKit 入口、LINE 複製、通知及更新安全：95 通過、3 跳過、0 失敗。屬本機自動化，不冒稱所有真實角色／裝置完整驗收。

## 發布邊界

Hosting-only。Functions、Firestore Rules、IAM、資料 ID、課表與計費資料格式不變；沒有帶入隔離測試按鈕或新角色分片發布。不能把此版稱為全部新同步架構已完成、全員即時送達或固定 2.5 秒。

正式舊分頁仍保留其原程式，並不強制刷新未存表單。IndexedDB 復原依相同分頁範圍；清除網站儲存空間或遺失分頁工作階段不在本次重開驗證範圍。

證據檔位於 /private/tmp/danbridge-308-draft-blocked-cloud.json、danbridge-308-draft-reloaded-cloud.json、danbridge-308-restored-cloud.json、danbridge-309-recovery-tests.log、danbridge-309-full-regression-r2.log、danbridge-309-extra-regression.log、danbridge-309-browser-draft.log、danbridge-309-browser-regression.log。
