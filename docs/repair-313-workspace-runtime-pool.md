# Staging 執行器重用驗收（尚未正式切換）

## 312 實際操作結果

- 正式前端維持 20.26.312，Git main `ce2dd91`；正式課表後端、Rules 與業務資料未更動。
- 使用左側 Safari 已登入 Daniel 的既有隔離工作區，前端 20.26.310、新 staging 後端 revision `stagingpublishedworkspaceoperation-00031-yug`。
- 單選 2026-09-29 08:00–08:15 合成課程，預覽往後一天正確阻擋學生撞課；改為往前一天，預覽 1 堂有效、0 衝突後送出，再按復原。
- 頁面實測操作至確認：9639 ms／8434 ms。**未達 2.5 秒**，且不含接收端繪製時間。
- 後端兩次 authority-read：4971／4441 ms；交易總耗時 6651／5925 ms，提交尾段 393／271 ms。
- revision 4401 → 4403 → 4405。124 堂課完整紀錄雜湊復原為 `record-v1:0d47c04eb61d9c924d88003608bbc58de9235a0b110b5e5f0845c75bbaceb8c5`，16 集合權威雜湊／數量核對成功。
- 每次操作四名接收者均有一份相同 revision 的單堂通知；AA／老師的角色投影與權威資料重建結果相符。這是伺服器讀回，**不等於四帳號瀏覽器實際已收到／已讀**。

## 313 變更邊界

僅 staging 合成驗收函式重用執行器。依原生 Firestore client、精確工作區 UUID、Owner／scheduler 類型與相容模式分隔，最多 4 個執行器、閒置 5 分鐘淘汰。失敗初始化可重試；重疊建構共用同一 Promise。

不快取登入身分、權限、工作區啟用狀態或權威雜湊。每次 execute 仍驗證身分、活躍成員、角色版本；每次原生交易仍執行工作區／角色 fence、完整歷程 ID 與 updateTime 查核、完整資料雜湊驗證及 lease 驗證。正式執行路徑不引用此 pool。

本機 unit 2 項及 native Firestore 測試 5 項通過；native 包含既有 40 筆新增／移動／刪除、角色撤銷、通知原子性及封閉工作區拒絕重送。後續仍需 staging 暖機後實測時間與四帳號瀏覽器驗證，通過前不切正式課表後端。

候選原始碼版本一致標為 20.26.313；本次只部署隔離後端，不部署前端、不聲稱正式網站為 313。
