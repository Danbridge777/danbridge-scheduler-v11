# 20.26.307：連續復原／重做歷史順序修補

基底為已發布的 20.26.303（c04e7b4e1aecd1249b53312ce60f5fd5cc128cc1）。本版只修正共用三向合併器的追加歷史順序，並替三個使用者加入明確版本匯入。其他瀏覽器差異僅版本標記。沒有改計費、薪資、角色規則或通知收件對象；不部署 Functions、Firestore Rules、IAM 或新傳輸架構。

## 原因與修正

操作歷史在 UI 為新到舊。舊合併器把回條等待期間產生的新歷史放到已提交歷史後方，下一次寫入因此被永久歷史防覆寫檢查拒絕。改為「新追加項目＋完整已提交歷史」，不刪除、不修改或重排已提交尾段。不移除防覆寫檢查。

隔離 305 頁面曾再次出現舊順序；當時未取得實際模組版本，不能斷言是快取造成。306 將三個匯入加上版本，並在隔離面板顯示實際核心 `newest-first-v1`，之後下列 Safari 實測通過。307 移植相同修正至乾淨正式基底，不帶入隔離診斷面板。

## 實際 Safari／Firebase 隔離驗收

- 四個真實已登入帳號：Daniel、AA、張毅、Catherine。只操作使用者指定的左側 Safari 視窗。
- 8 堂：移動→復原→重做→復原→重做；完整課程內容回到原始基準，revision 4145。
- 20 堂：移動→復原→重做→復原；124 堂完整課程內容與 ID 集合皆與初始基準相同，revision 4305。
- 四帳號均實際打開最新 20 筆通知並按「知道了」；獨立雲端讀回四筆通知皆 `read:true`、20 個相異 lesson ID。
- UI 匯出與雲端 1911 筆完整歷史逐筆比對，數量與順序完全一致（mismatchCount 0）。AA 331 分片、老師 100 分片與權限投影一致。
- 正式資料寫入 0；所有操作僅在固定合成驗收命名空間。沒有新增需要刪除的課程複本。
- 這不是瞬間點擊間距或 120Hz 證明；原生操作工具存在操作間隔。單元測試另以延遲回條注入連續操作驗證併發情境。
- 同步實測約 6–9 秒，未達 2.5 秒。使用者已取消秒數作為這次獨立正確性修正的發布門檻。

證據：`/private/tmp/danbridge-306-after-five-actions.json`、`/private/tmp/danbridge-306-twenty-live-readback.json`、`/private/tmp/danbridge-306-twenty-live-ack-history.json`。

## 發布前測試與待完成事項

- 307 獨立新增的 1／8／20 堂延遲回條＋復原／重做測試 3 項通過；測試也確認真的改寫舊歷史仍被拒絕。
- 全套回歸 1138 項（1131 通過、7 略過、0 失敗）；另加本版 3 項歷史順序測試皆通過。Chromium/WebKit 76 通過、2 裝置條件略過。驗證 149 個本機引用、467 個 JavaScript、364 個 HTML ID。
- 共用 staging Hosting 部署被審查拒絕，沒有重送。改用獨立 `history-307` 預覽頻道；43 個資產與本機 SHA-256 全部一致，不覆蓋一般 staging 或 published-280 驗收頻道。
- 發布前 production 仍為 303、41 資產一致；Rules `ad7ddf44-0ef1-42d4-b534-bb03291f9e87`、兩個正式函式 revision 與 activationEpoch 未變。健康告警快照仍為 `attention`（2026-09-11T19:17），不宣稱 healthy。
- 新傳輸架構尚未發布；被拒絕、尚未進入日誌的舊草稿在重新載入時之恢復仍待專項驗收。305 失敗草稿已匯出保留，沒有用清除快取抹掉證據。
