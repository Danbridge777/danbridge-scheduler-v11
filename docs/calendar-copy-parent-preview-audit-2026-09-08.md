# 課表首次複製、家長預覽與學生排課預設：發布驗證

基準提交：bbe2a6d。日期：2026-09-08。發布候選：20.26.251。以下記錄本機與 staging 驗證；production 發布結果另由部署回讀確認。

## 已重現與修正

- 搜尋欄持有焦點後，滑鼠框選課程會 preventDefault，原先未將焦點轉回課表；鍵盤處理器因事件目標仍是 input 而略過複製。修正前 Ctrl+C、Command+C 瀏覽器案例均失敗於未出現貼上提示；修正後通過。
- 在開始框選及切換課程選取時，將焦點移到課表容器（preventScroll）。不改寫同步、課程 ID 或薪資公式。
- 家長提示依 lesson.studentId 精確讀取學生；同名不合併，缺家長顯示「尚未填寫」，重複 ID 不猜測。提示使用 textContent，保留現有非老師角色的家長資訊邊界。
- 覆蓋月／週課程卡、課程詳情標題、批次預覽學生姓名，以及學生選擇欄位目前選定值。原生下拉選單中每個尚未選取選項的滑鼠提示，尚未實作自訂選單驗收。

## 已執行

- calendar-first-copy.spec.js（含新增學生帶入）：Chromium、WebKit、Windows user-agent Chromium，共 30/30 通過。
- 包含第一次 Ctrl+C／Command+C、搜尋後框選、排課角色三次連續複製、普通老師不複製、文字欄位不攔截、同名家長與 HTML 字元安全、提示邊界、切角色隱藏、頁面重新載入後三輪各八堂課複製。
- 同次前一輪 finance-month-payroll.spec.js 三個桌面設定均通過；另薪資與課表呈現測試 9/9 通過。
- 已查看產生的月課表家長提示截圖。
- 新增學生帶入與切換學生案例：iPad WebKit、mobile WebKit、Android Chromium 模擬共 6/6 通過。
- npm test 完整生命週期（pretest/test/posttest）退出碼 0。
- staging 已部署前端，index、sw、課表呈現、課程操作、框選及業務模組六個檔案與本機 SHA-256 一致。
- staging 網址載入的隔離瀏覽器回歸：課表新功能加月份／薪資檢查，三個桌面設定共 33/33 通過。
- 真實 staging Daniel 已登入：透過介面新增課程、選 STAGING_SHADOW_STUDENT，實際顯示來源 2026-09-07，老師 STAGING_SHADOW_TEACHER、課程名稱與 30 分鐘時長正確帶入（16:00→16:30）；關閉未儲存表單。選課後只按一次 Command+C 出現「已複製 1 堂課」，再 Esc 取消。沒有按儲存、貼上或刪除。

## 明確限制

發布補充：新增課程預設帶入以學生 ID 綁定、排除取消／停課／草稿／未來課程，偏好老師與單一校區優先；教室只帶入仍屬於該校區的教室，缺設定不猜測。新增測試確認原日期／開始時間保留、時長帶入、同名隔離、既有課程不修改、付款與回報不複製、學生切換清除上個學生的地址。桌面三配置共 30/30 通過。

自動化瀏覽器案例使用隔離身分及記憶體測試資料，不是真實 Google 登入或 Firestore 雲端同步驗收；真實登入的只讀 UI 操作範圍如上。Windows 為 user-agent／viewport 模擬，不是 Windows 實機。尚未驗證生產站首次登入、登出登入、離線重連或真實三帳號同步。沒有修改正式資料；以上結果不能代表完整安全稽核已完成或任何情況都保證成功。
