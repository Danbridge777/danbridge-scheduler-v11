# 327 candidate — report write path repair

Production remains 322. No production deployment in this work sequence.

Safari reconnected on 2026-09-15. The retained teacher dialog showed a real `permission-denied` report failure, not a confirmed App Check rejection. Its 326 module URL was visible in the native console. Report text remained in the form. Earlier automated input had landed homework in content and feedback in homework; these are disposable test fields and must be filled and reread individually on the repaired page.

Read-only staging evidence:

- Rules release `58a8ec81-0afd-4723-b249-8e995e8206d6`, source SHA-256 `b5f1278ece549e34a0b5914518cc5706d3cb6ccc5e4ceb456abea927226a69ed`.
- The report create/update branch is gated by the legacy write-open function. The permanent V1 fence exists with state `permanently-fenced-after-atomic-v2-structural-activation`.
- Exact test lesson metadata is active, bound to the authorized staging teacher, with report window 2026-09-14T16:00:00Z through 2026-09-15T15:59:59.999Z. The test report GET returned 404.
- Existing browser `saveTeacherReport` still used direct Firestore `setDoc`. New candidate uses a separate guarded callable. Browser write-denial Rules are unchanged.
- Historical sanitized App Check evidence on this teacher tab contains two 401 entries at 2026-09-14T14:49:59.267Z and 14:50:29.386Z, both unclassified. This does not establish the historical underlying attestation root cause. A reCAPTCHA PAT endpoint 401 is not automatically the same as an App Check token-exchange 401.

Candidate invariants: current verified auth and fresh App Check, exact project binding, active membership, trusted lesson metadata, teacher/branch/date scope, strict payload, optimistic report version, idempotent operation receipt, identity-safe frontend result handling. The attempted teacher array-contains query was rejected by the exact production Rules in the emulator. It was not shipped. Teachers instead read one exact lesson report through the same guarded callable, which rechecks live scope and performs no writes for readOnly requests. Incoming snapshots no longer replace unsaved report forms. Existing owner journaling is preserved. The isolated published workspace fails closed for this new report operation until separately wired.

Tests: final targeted run 61 passed, zero failures/skips. Real localhost Firestore concurrent transaction / lost-response replay / revocation and exact production Rules scope tests: 2 passed, zero failures/skips; emulator stopped. Final full regression: 1,493 executions, 1,483 passed, zero failures, 10 skipped. Old direct-write assertion replaced by confirmed-backend-before-local-application and exact scoped read invariants; teacher collection subscription remains disabled. Staging callable and Hosting 327 deployments completed successfully on 2026-09-15. Production and Rules unchanged.

Pending: teacher UI save/reload and owner readback; remaining four-role calendar/notification tests; disposable lesson cleanup; final production eligibility. No claim of four-role completion or historical 401/403 root-cause resolution.

## 328 integration correction

Native Safari loading 327 exposed `Error: 排課 callable client 設定無效` in `firebase-callable-http-client.js:23:315`. The newly wired report endpoint was missing from that client's exact project/function allowlist and aborted auth-module initialization. This was a candidate regression, not a network or App Check root cause. Added only the staging/production report endpoint pairs and tests constructing every configured callable name, rejecting all cross-project pairs and verifying Auth/App Check headers for both report endpoints. No security protections disabled. 328 full regression completed with exit 0; live acceptance remains required.

328 full regression: 1,495 executions, 1,485 passed, zero failures, 10 skipped. Staging Hosting 328 deployed; three key public module bodies matched local SHA-256. First Safari 328 load failed with 27 resource errors including `Service Worker context closed`, not an App Check HTTP rejection. Normal reload recovered without clearing storage or credentials; teacher cloud initialization briefly exceeded 20 seconds before reaching verified synchronized state. At 2026-09-15T10:44:32.084Z, native teacher UI successfully saved the exact retained test lesson report. Independent staging GET confirmed completed status, content `AUDIT328 content`, homework `AUDIT328 homework`, feedback `AUDIT328 feedback`, note `AUDIT328 note`, teacher email `yamiiii8549@gmail.com`. No production changes.

## 329 current staging acceptance — not production eligible

The teacher reopened the report and all four text fields matched. A second native save at 2026-09-15T10:46:31.049Z changed only homework to `AUDIT328 homework revised`; independent staging GET confirmed this exact result. Function logs corroborated successful Auth and App Check verification for the read/save requests. Whole-page reopening still showed zero completed reports despite the persisted report, so this is a separate unresolved display/read-path acceptance defect, not lost cloud data.

329 adds a bounded read-through hydration path for the teacher's displayed months. Each exact lesson read rechecks live scope; batch maximum is 40, with no cloud writes. Identity/reset fencing prevents late old-account results applying, and stale report versions cannot replace newer report data. Teacher broad collection subscription and Firestore Rules remain unchanged. Full regression: 1,498 executions, 1,488 passed, zero failed, 10 skipped. Two emulator tests passed. Two additional queue/reset tests then passed in the 35-test report suite. Staging function revision `stagingsavelessonreport-00003-yof` and Hosting 329 deployed successfully. Four public module hashes matched local candidates, including auth module `69bd98fe188943e22af24b065950796d97f96020981b0c91e69650e146e3ac98` and hydrator `633a61d1dd11a2e575b35030aa372e3c7ddb2fbe3d94451229a36518a07b9855`.

AA's retained 326 page failed another real notification acknowledgement: cloud read remained false and UI restored the unread notification with a 38m48s App Check cooldown. Native console showed repeated exchange 401s during the background session; sanitized stored evidence classified the 2026-09-15T09:21:55.406Z event as `attestation-retryable`, visibility hidden, online true. Older logs also contained network disconnections. Neither the network errors nor hidden visibility alone establishes the attestation root cause. No throttle reset, security bypass, or forced acknowledgement was performed. Four delivery documents for the exact disposable lesson still exist, but delivery is not acknowledgement.

At 11:08Z the exact disposable lesson remained active, revision 1, date 2026-09-15, 16:00–16:30. No business data deleted. A 329-URL teacher visit still displayed zero completed reports; the actual executing version and hydration outcome must be verified, because a pending normal PWA update was also visible. Native control was intermittently interrupted by tab switches, clipboard timeout and `noWindowsAvailable`; AX button operations subsequently resumed. A user-facing normal PWA update was selected on the saved teacher test page. Its result is pending; do not count it as a pass.

Production remains 322. No production deploy, Rules change, GitHub push or claim that historical 401/403 is resolved. Remaining gates: teacher reload/dashboard agreement, four-role real operations and acknowledgement, safe cleanup of the retained disposable lesson, and earlier remaining acceptance cases.

Follow-up native result: the normal PWA update completed and the teacher dashboard now shows 16 lessons, **1 reported**, **6 pending**, 9.0 hours. Reopening the exact test report displays the four expected text fields, including `AUDIT328 homework revised`. No extra save was made. Thus the updated-page report hydration/display case passed; the earlier pre-update view is retained as separate evidence. Next, AA's existing same-origin recovery link was clicked, leaving the rejected page intact. Its fresh-page result remains to be checked.

AA follow-up: recovery opened the exact same-origin root without removing the old failed page. A normal pending PWA update was accepted on this new saved page. The exact 2026-09-15 16:00–16:30 added notification was opened and acknowledged through the UI. Count decreased 225 → 224. Independent Firestore read at 2026-09-15T11:26:42.960742Z confirmed AA `read:true`, acknowledgedAt `2026-09-15T11:26:20.795Z`; teacher read remains true, Catherine and Daniel still false at that read. This is a recovery success, not a claim that the retained-tab repeated 401 root cause is resolved. The report function also logged valid Auth and App Check at 11:16:42Z and 11:18:08Z for the teacher's updated-page report flow. Continuing with owner accounts through ordinary Google sign-in.

Historical handoff checkpoint: AA new page was signed out through the normal UI; Google sign-in selected the already authorized Catherine account. The popup reached the Firebase callback, followed by control timeouts. Subsequent observations below supersede this pending-login checkpoint. No new credentials entered and no consent/security expansion occurred.

## Catherine / teacher continuation and 330 worker candidate

Native Safari subsequently confirmed owner Catherine. She opened the exact disposable lesson's added notification and acknowledged it; independent cloud read confirmed `2026-09-15T11:40:10.048Z`. Through the real calendar editor, only the note input was edited to `AUDIT329 Catherine sync verification` and saved. Cloud lesson revision became 2 and four modified notices were created at `11:42:56.142Z`. The editor also committed the completed status inherited from the earlier teacher report; the notification describes 未上課 → 已上課. Therefore this is not claimed as a cloud note-only mutation.

Catherine acknowledged the modified notice at `11:44:07.460Z`. The independent teacher page received it without a manual reload. An initial action was interrupted by the user switching to Notion and was not counted. Returning to the teacher page and clicking the real acknowledgement succeeded at `11:46:10.840Z`, independently read back at `11:47:23.347742Z`. Daniel added/modified and AA modified acknowledgements remain pending. Exact staging lesson `lsn_69253a01-9b05-41d1-b759-146a9090ac98` remains active, revision 2, pending final cases and authorized cleanup. No production business data touched.

After Catherine's normal logout, Safari's sign-in page failed to load. Native Inspector displayed 57 errors including repeated `Service Worker context closed`, followed by missing `db` globals; login remained disabled until the page's timeout message. This is an observed resource-loading failure, distinct from an HTTP App Check rejection. A later normal reload also remained at the disabled login state. No storage, credentials, attestation controls or Rules were cleared or bypassed.

330 keeps the Service Worker registration URL stable (`./sw.js`, `updateViaCache:none`), tracks asynchronous cache writes and skipWaiting with event.waitUntil, absorbs storage/background-fetch failures, avoids caching non-OK HTML, and returns explicit network errors when no offline copy exists. Existing user-approved-update / pending-draft guards remain. Firebase reserved auth routes, cross-origin requests, and non-GET traffic remain outside worker interception. These defects are independently testable but are not claimed as the unique root cause of historical 401/403 or the browser's worker shutdown.

Targeted worker/PWA/leave tests: 37 passed. The first full runs correctly caught obsolete version/registration assertions; these were updated to assert release 330 and the new stable-registration invariant, without removing coverage. The subsequent full npm test completed with exit 0. Staging Hosting deployment and real 330 Safari acceptance are next; production remains 322.

330 Hosting subsequently deployed successfully (362 files, 20 uploaded). Public SHA checks matched local: sw `7fd6ece39857edbf35d2933ca26935723eef73cb7579f23bf3a8ccd2ef2490ef`, pwa module `82c84af764e9cba32f05ab2e3853865438aa0d8d95a78e626649edefd8138807`, auth module `27a96b53ce111454bb86925003d596a55824df1842ec14c7e4350c1c9c9711ca`. Full test totals: 1,511 executions, 1,501 passed, zero failed, 10 skipped. Native Safari confirmed executing auth and PWA script URLs 330. The active worker still reported the earlier versioned 329 URL with no waiting worker, so do not claim the stable-URL worker has become active solely from those script URLs. Fetching the PWA script in the same page confirmed the new updateViaCache setting. Historical old pages remain open. No manual worker unregister, storage clear, or synthetic activation occurred.

AA's exact original modified notification was acknowledged through UI at `2026-09-15T12:12:09.129Z`, independently read back. Following normal reload the user identity was AA (confirmed by that cloud recipient), not the previously assumed Catherine; no operation was attributed to Catherine merely from the tab's prior identity. An accidental incomplete native console diagnostic input produced a ReferenceError and was corrected after verifying the full input; this is tool input failure, not an application defect.

AA native calendar cases on 330: copy original → paste on 9/16; select 2 → copy/paste onto 9/17–18; select 4 → copy/paste onto 9/19–22. The four-copy action was initially aborted by the user's Notion switch; only retried after seeing the clipboard still pending and no new rows. Independent cloud read at `12:29:44.097547Z` confirmed exactly 8 active matching-title records with unique IDs, consecutive 9/15–22 dates, 16:00–16:30, and all four recipients' added notices (batches 1, 2, 4).

AA selected those exact 8 lessons, previewed +30 minutes (8 valid, 0 conflict), applied, then performed undo and redo. The first undo attempt was aborted by another Notion tab switch and not counted. Cloud read at `12:33:25.909617Z` confirmed all 8 at 16:30–17:00, original revision 5 and copies revision 4; three corresponding eight-item modified-notice batches existed for all four roles. These are UI and cloud-order correctness checks, not claims of 120Hz or measured end-to-end 2.5 seconds.

Exact title search was verified before selecting all 8. Native mouse drag from 9/15 to 9/16 moved all 8 forward one day. Cloud read at `12:37:34.892496Z` confirmed dates 9/16–23, original revision 6 / copies revision 5, and four complete eight-item notifications. Native undo returned the visible dates; final readback is pending. AA then signed out normally, login became enabled, and the normal Google popup was opened for Daniel. Account selection and final identity are still pending at this checkpoint.

Disposable set now contains original plus seven copies, all exactly titled AUDIT326-FOURROLE-AA-20260915. IDs: lsn_69253a01-9b05-41d1-b759-146a9090ac98, lsn_c2856f0c-03d2-4af2-b6d1-d55f04c43cb4, lsn_53b30ceb-1d49-460b-a14f-a1f168b6a244, lsn_ba8de981-aafd-4c83-b3a2-39f97bad189f, lsn_ec5eb91b-f484-4ecf-bb1c-a211d2b1e279, lsn_fb6e7675-a2e6-413c-b93b-c8974338c157, lsn_4bd89106-b0fd-4b37-8836-d4582e94aae2, lsn_c55be771-8087-493c-9591-acdf10345ec0. Clean up through authorized staging UI only after remaining receiver cases. Other STAGING_SHADOW lessons predate this sequence and are not included.

Daniel completed Google's existing passkey verification himself. Native Safari then confirmed owner Daniel. He acknowledged the teacher report notice (dismissal observed; its cloud acknowledgement has not yet been independently checked) and the eight-lesson undo notice. Read-only cloud verification at `2026-09-15T12:51:03.997830Z` confirmed all eight dates restored to 9/15–22, 16:30–17:00, original revision 7 / copies revision 6; Daniel acknowledgement was `12:50:45.612Z`. His preceding eight-lesson drag acknowledgement was independently confirmed at `12:51:38.971Z`.

Latest eight-lesson undo notification `2026-09-15T12:37:53.185Z` now has all four actual UI acknowledgements independently verified: Daniel `12:50:45.612Z`, teacher `12:53:03.374Z`, Catherine `12:59:42.022Z`, AA `13:07:21.859Z`. Catherine signed in normally without another challenge; AA's existing passkey required user Touch ID, which the user completed. No credential or security changes.

AA used the exact-title filter, selected exactly eight, confirmed deletion, undid it, and redid it. Intermediate cloud read `13:09:46.173023Z` confirmed all eight restored under their original IDs (original revision 9 / copies 8). Final read `13:12:38.647117Z` confirmed zero active and eight soft-deleted records (original revision 10 / copies 9); four matching eight-item removed notifications exist. Teacher's existing page automatically became 15 lessons / 8.5 hours / no current-day report, matching removal of eight half-hour lessons. The original report is not erased as part of soft deletion. No other STAGING_SHADOW data or production data deleted. Final teacher removal acknowledgement was clicked; independent readback remains pending.

Safari's dedicated service-worker inspector directly displayed the running web.app worker source at the versioned 329 URL, including 329 APP_SHELL and the pre-330 untracked cache-write implementation. Therefore this is actual old worker execution, not only an old URL label. The current page's registered `load` listener was read via native Inspector and contains the correct 330 stable `register('./sw.js',{scope:'./',updateViaCache:'none'})` line. A normal saved-page reload with Inspector open is being used to diagnose why activation has not followed. Do not claim worker acceptance complete or infer that this proves the 401/403 root cause.

The existing independent teacher page already showed 23 monthly lessons, 12.5 hours and today's exact test at 16:30–17:00 without manual reload. Teacher acknowledged the copied one-lesson notice, then the latest eight-lesson undo (`12:53:03.374Z`) and drag (`12:54:39.740Z`), both independently verified by cloud read at `12:55:38.836719Z`. The normal PWA update initially respected its open-dialog guard; after closing the notice, the update completed via the normal entry. Reopened dashboard retained 23 lessons / one reported / 12.5 hours, and opening the exact original report retained all four expected fields, including revised homework. No report save occurred. Another update banner remained visible, so stable worker activation still requires inspection. Daniel then signed out normally and the login button became enabled; Catherine's normal Google popup is in progress. No production changes.

## Final 330 checkpoint — installation gate remains open

The preceding paragraphs are chronological checkpoints, not unresolved duplicate login requests. All four same-batch acknowledgements and the eight-lesson cleanup above supersede the earlier pending account checkpoints.

After preserving the failed-page diagnostics and verifying no unsaved form, two obsolete staging tabs were closed normally. The saved AA page was also closed and a clean staging root opened. AA remained signed in through the ordinary persisted session. Registration still had the actual 329 worker active/controller, a stable-URL worker installing, and no waiting worker. A subsequent read-only `caches.keys()` resolved with one cache; this rules out that particular cache enumeration call hanging, but does not locate the stalled installation operation. Public APP_SHELL resources returned HTTP 200 in separate CLI checks; that does not prove Safari's own precache requests completed. No unregister, cache deletion, credential reset, automatic skipWaiting or security bypass was used. A user handoff requested saving other Safari tabs and a normal complete Safari restart, without clearing website data. Restart completion is not yet verified.

Read-only Cloud Logging sample from 12:00Z returned 150 entries with more older entries available. Returned callable verification records included Auth VALID / App Check VALID for the four acknowledgement times, scheduler delete/undo/redo and teacher report reads. No HTTP >=400 or ERROR was observed within those returned entries. This is a bounded sample, not an assertion about all historical requests, and it does not establish the historical 401/403 attestation cause.

Read-only Firestore administration check around 13:32Z confirmed production PITR_ENABLED and DELETE_PROTECTION_ENABLED, with earliestVersionTime 2026-09-08T13:32:00Z. No TTL policies were returned and there was no next page. Staging PITR and deletion protection were disabled, also with no TTL policies. No settings changed. These are retention/protection configuration observations, not proof of a one-year cloud restore drill.

Additional targeted regression rerun: 93 tests passed, zero failed/skipped, covering PWA update safety, scoped lesson reports, callable transport, lossless backup import, sharded backup and immutable migration backup. This does not replace the outstanding native worker installation gate or the annual real restore acceptance.

Production remains 322. No 330 production deployment or GitHub push has been performed.

Further native receiver acceptance completed: the final eight-item removal notice created at `2026-09-15T13:10:01.177Z` is now independently confirmed read for all four accounts. Teacher acknowledged at `13:13:43.714Z`, AA `13:41:21.256Z`, Catherine `13:45:12.596Z`, Daniel `13:50:28.432Z`. Final readTime `13:50:50.959359Z` also confirms zero active / eight soft-deleted matching test lessons. This is an additional same-batch deletion-notification case, distinct from the earlier four-role movement notice.

Catherine and Daniel both completed ordinary Google account selection without another biometric handoff during this final receiver pass. Catherine initially displayed the 20-second cloud-confirmation timeout warning, then completed initialization and acknowledged successfully; therefore do not count this startup as meeting a 20-second or 2.5-second timing target. Native Inspector opened after initialization had no captured errors; it cannot retrospectively prove there were no earlier errors. Daniel's role label was explicitly observed as owner Daniel before acknowledging. No production data, security settings or credentials were changed. Safari complete-restart / stable-worker activation and historical 401/403 root-cause gates remain open.

User explicitly authorized the agent to restart Safari. A normal Command-Q ended the app; the running-app inventory no longer contained Safari. Reopening Safari and selecting History → Reopen All Windows From Last Session restored the ten prior tabs. Daniel's persisted login remained valid. The previously stuck stable-URL worker had now completed installation and appeared waiting, with no installing worker. After closing the notice and using the normal guarded “立即更新” button, the native console independently returned ACTIVE `https://danbridge-d8877-staging.web.app/sw.js` state `activated`, WAITING undefined, INSTALLING undefined and matching CONTROL URL. This passes stable-worker activation after a complete normal browser restart; it does not prove the underlying Safari installation stall or historical App Check attestation rejection cause. No website data or credentials were cleared. Network console also displayed Firestore listen “access control checks” errors; these are preserved as separate unresolved observations, not silently dismissed or relabeled App Check 401/403.

The independent teacher origin was also inspected after restart: active worker `https://danbridge-d8877-staging.firebaseapp.com/sw.js`, state activated, no waiting/installing worker. Dashboard remained 15 lessons / 8.5 hours. A delayed native clipboard insertion temporarily produced duplicated console diagnostic text and a SyntaxError; the input was cleared and the exact read-only query re-entered. This was a tool input error, not application code.

Post-restart, post-update same-batch notification acceptance: eight-item restore/addition notice created `2026-09-15T13:09:21.419Z` was acknowledged using each actual role UI. Teacher `14:06:46.691Z`, Daniel `14:07:31.300Z`, Catherine `14:13:07.746Z`, AA `14:17:50.907Z`; independent cloud readTime `14:18:12.577303Z` confirmed all four read true, zero active / eight deleted test lessons unchanged. Catherine and AA used ordinary Google sign-in with their existing authorizations. No impersonation, synthetic client write or forced acknowledgement was used.

Additional App Check / scoped report regression: 61 passed, zero failed/skipped. Includes preserving original 403 and the SDK backoff, retrying 401 only after SDK backoff with fresh mocked attestations, retaining exact operation IDs, denying stale identity/cross-project/unauthorized roles, and no token replay. Mocked rejection tests are not historical live root-cause proof.

Read-only reCAPTCHA getMetrics returned 90 daily aggregates starting 2026-06-17T07:00:00Z. The last three returned days had 980, 41, 0 scored assessments respectively, all nonzero entries in the 0.9 bucket. These aggregates do not identify any individual failed request and do not cover the missing rejection detail. Cloud Logging query for reCAPTCHA service audit entries since 2026-09-14 returned zero entries, no next page. The first diagnostic Logging call used an incorrect client argument envelope and returned HTTP 400; the corrected read-only query succeeded. No logging policy, risk threshold or security configuration was modified.

The original release condition included establishing the historical 401/403 cause. Since that evidence is unavailable despite the new four-role passes, a user choice was requested: publish verified repairs while explicitly retaining the historical unknown, or keep production 322 pending root-cause investigation. Production remains unchanged while that changed release condition is unanswered.

Release preflight: the first Node all-asset check hit a connect timeout and was not counted. The retry with per-process IPv4-first DNS completed: all 357 browser assets on staging 20.26.330 matched local SHA-256, zero mismatches, zero data writes. No browser or OS networking/security setting changed. Production read-only health check at `2026-09-15T14:19:23.831Z` showed healthy, zero pending requests, zero recorded recent errors (untruncated), all seven roles using chunk-only data, maximum role document 77,257 bytes against 800,000-byte safety budget, enabled PITR/deletion protection, and enabled daily maintenance. These are exact bounded metadata checks, not complete feature or historical-failure proofs.

An independent curl-backed read-only comparison also completed all 357 public staging assets with zero SHA mismatches. Final `git diff --check` and full `npm test` (including pretest and posttest, shell pipefail enabled) both exited 0. The final displayed App Check/ack sub-suite was 38 passed, zero failed/skipped; do not mislabel this sub-suite count as the total full-run count. No candidate code changed during these checks. No production deployment or GitHub push occurred. Awaiting the explicit release-condition choice, not another browser/login authorization.
