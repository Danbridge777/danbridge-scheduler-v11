# Danbridge 功能清單與驗收狀態看板

最後盤點：2026-08-08
基準分支：`main`  
狀態依據：目前程式碼、Firebase 設定、`tools/validate_project.py`、`tools/audit_scenarios.js` 與正式站既有操作結果。

## 狀態定義

| 狀態 | 定義 |
|---|---|
| 已自動驗收 | 有可重複執行的測試，且目前通過。 |
| 已人工驗收 | 已在正式站依明確步驟操作通過，但仍應補自動測試。 |
| 部分完成 | 功能已存在，但缺少關鍵邊界、測試、稽核或營運程序。 |
| 未開始 | 尚無可驗收的實作。 |
| 需要決策 | 需由 Owner 決定政策、範圍或外部服務後才能實作。 |

完成一項功能至少需要：驗收條件、程式或設定、正常／錯誤／權限案例、靜態檢查、本機 commit、正式部署與正式站煙霧測試。

## P0：正式營運前必要項目

| ID | 功能 | 驗收條件 | 現況 | 下一個動作 |
|---|---|---|---|---|
| P0-01 | Owner、老師、校區管理者權限 | 三種角色逐一驗證讀取、禁止寫入、老師本人回報、跨老師／跨校區拒絕；前端與 Firestore Rules 結果一致。 | **已自動驗收：** Firestore Emulator 共 11 項測試通過，涵蓋匿名、停權、Owner、老師、校區管理者、回報範圍防偽、通知欄位限制與雙客戶端即時同步。 | 每次修改 Rules 或同步流程必須執行 `npm run test:rules`。 |
| P0-02 | 課表核心操作 | 單堂、多選、框選、全選、複製貼上、週複製與月複製皆限定目前老師；月／週拖放立即落位並同步。 | 部分完成：單堂／多堂拖曳、框選終點、老師限定剪貼簿、實際貼上落位、批次候選、週複製、月複製與選取複製已有自動情境；老師範圍與連續拖曳已人工驗收。 | 建立 staging 後補完整瀏覽器操作與失敗重試測試。 |
| P0-03 | 衝堂判斷 | 空白日期不誤判；同日、重疊時間且相同學生、老師或教室才阻止；錯誤訊息指出衝突來源。 | **已自動驗收：** 不同日期、相鄰時段、相同學生、同教室、不同學生與教室、取消課程、跨午夜拒絕、共同授課老師與衝突訊息來源均有自動邊界測試。老師重疊維持警告，學生與教室重疊才阻止。 | 每次修改時間、老師、學生、教室或拖曳流程時執行 `npm test`。 |
| P0-04 | Firebase 同步一致性 | 寫入後本機立即顯示；雲端確認前舊 snapshot 不得倒灌；另一登入角色在合理時間內收到更新；失敗可見且可重試。 | 部分完成：本機立即落位、Firebase Emulator 雙客戶端即時同步、舊 snapshot 防倒灌、同步途中較新本機修改續傳，以及失敗後可見的指數重試均已自動驗收。 | 補上 staging 的 Owner／老師或校區管理者雙角色瀏覽器操作，驗證實際畫面更新時間與重連。 |
| P0-05 | 備份與還原 | 可匯出完整資料；在非正式環境還原；比對各集合筆數、主要關聯與抽樣資料；留下演練日期與結果。 | **已自動驗收：** Backup V3 包含 16 個集合筆數與校驗碼；2026-08-08 已在獨立 Firestore Emulator 完成匯出、清空、還原、集合筆數、主要關聯與完整快照比對。 | 每次修改資料結構後執行 `npm run test:rules`；正式資料仍維持只匯出、不在正式環境測試還原。 |
| P0-06 | 錯誤監控 | 前端未捕捉錯誤、Firebase 寫入失敗與角色 view 發布失敗可集中查詢；不記錄敏感資料；可對應版本。 | **已完成最小版本：** 未捕捉錯誤、主要雲端讀寫與角色 view 發布失敗會寫入隔離的 `errorEvents`；固定 schema、去重、單次上限與 4 項專屬 Rules 隱私案例避免敏感資料外洩。 | Owner 定期依版本、環境、類型與區域查詢；後續設定 30 天自動清理。 |
| P0-07 | 發布與回滾 | 每次發布有檢查結果、commit、版本與 Hosting 確認；可在 15 分鐘內回到上一穩定版。 | **已完成：** 已建立發布清單；2026-08-08 在 staging 實際完成 `20.7.1 → 20.5.9 → 20.7.1` 回滾與恢復，兩次均少於 5 分鐘，恢復後 SHA-256 與演練前一致。 | 每次正式發布建立 commit、版本 tag、檢查紀錄與線上資源確認；正式站異常時依清單處理。 |

## P1：完整營運功能

| ID | 模組 | 驗收條件摘要 | 現況 |
|---|---|---|---|
| P1-01 | 帳號管理 | 邀請、停權、角色變更、老師／校區綁定與最後登入；變更立即生效。 | 部分完成：授權資料與綁定存在，完整生命週期與自動權限測試未完成。 |
| P1-02 | 學生 CRM | 唯一識別、重複偵測、歷程、封存／恢復且不破壞課程關聯。 | 部分完成：封存／恢復已保留歷史課程、收款與月結關聯，並從新排課選項排除；完整欄位修改歷程與重複偵測仍待補。 |
| P1-03 | 老師人事 | 任教資料、工作日、薪資制度、共同授課、離職封存與公式版本。 | 已自動驗收：封存／恢復不刪除歷史課程、薪資與月結，封存時連動停權綁定帳號，恢復後帳號維持停權待 Owner 人工確認；薪資公式已有版本。 |
| P1-04 | 老師課堂回報 | 只回報本人課程；提交者、時間、修改歷程可稽核；草稿課不可回報。 | 部分完成：本人與時間窗規則存在；完整修改歷程與 Emulator 測試待補。 |
| P1-05 | 補課 | 原課、請假、補課可追溯；完成／刪除補課會正確更新待辦。 | 已自動驗收：目前 audit 覆蓋建立、取消、重開、完成與刪除後恢復；仍需裝置煙霧測試。 |
| P1-06 | 營隊 | 班級、多老師、跨日、報名、收款與課表一致。 | 部分完成：已有營隊與報名資料；跨日、收款一致性和權限驗收待補。 |
| P1-07 | 學生收費 | 應收、已收、免收、未收與調整可區分，金額可追溯來源。 | 部分完成：課程計費與收款紀錄存在；調整單與完整來源追蹤待補。 |
| P1-08 | 老師薪資 | 依課程、時數、薪資制度及是否計薪計算；公式有版本。 | 已自動驗收：正式課表工時、明確不計薪、共同授課、營隊同時段去重、固定薪／時薪、當月實際工作日比例、請假區間去重、跨月隔離、閏月與 100 位老師共 3,100 堂大量資料均有固定輸出；薪資結果與月結快照記錄 `teacher-payroll-v2-workday-leave` 公式版本。 |
| P1-09 | 月結鎖定 | 月結建立不可變快照；結算後改課產生調整單，不覆寫歷史。 | 已完成：同月同校區只可鎖定一次，原始數字不可覆寫或刪除；後續資料變更建立不重複的差額調整紀錄，復原變更會建立反向調整。 |
| P1-10 | 通知中心 | 課表異動、未回報、補課、未收款與工時提醒；去重、已讀與來源連結。 | 部分完成：課表新增／修改／取消依老師與校區合併通知，能返回原課表；已讀課表通知保留 30 天、未讀保留 90 天並由 Owner 分批清理。未回報限近 30 天、未收款限近 120 天且依 CRM 學生精準合併。課表異動的完整瀏覽器驗收仍待補。 |

## P2：平台化與擴充

| ID | 功能 | 驗收條件 | 現況 |
|---|---|---|---|
| P2-01 | staging 環境 | 使用獨立 Firebase 專案或完全隔離集合；測試不碰正式資料。 | 已完成：獨立 Firebase 專案、Web App、Firestore、Google 登入、Rules 與 Hosting 均已建立並完成煙霧測試。 |
| P2-02 | CI 自動檢查 | GitHub Actions 自動執行語法、專案驗證、情境測試與 Rules 測試。 | 已完成：push、PR 與手動執行會自動驗證專案與 manifest、執行情境測試、Rules Emulator 測試及相依套件安全掃描。 |
| P2-03 | 高風險後端寫入 | 月結、權限、批次操作由 Cloud Functions／受信任 API 驗證。 | 未開始。 |
| P2-04 | 背景工作 | 通知、備份、月結與報表可排程、重試與稽核。 | 未開始。 |
| P2-05 | 家長入口 | 家長只讀本人學生資料，可查看課表、回報、收款與通知。 | 未開始；需先完成 P0 權限、監控、備份與 staging。 |
| P2-06 | 線上付款與訊息 | 付款狀態可驗證、重複 webhook 安全、通知可追蹤。 | 未開始，需要決策服務商與費率。 |

## 非功能需求

| 項目 | 驗收條件 | 現況 |
|---|---|---|
| 可及性 | 表單標題與欄位可由閱讀輔助工具配對；搜尋、儲存與同步狀態可被宣告；新增的動態表單套用相同規則。 | 部分完成：既有與動態欄位會自動建立標籤關聯，無標題搜尋欄位具有可讀名稱，主要動態狀態使用禮貌宣告。完整鍵盤焦點順序與實機閱讀器驗收待補。 |

## 核心課表回歸看板

| 驗收案例 | 自動測試 | 正式站人工驗收 | 狀態 |
|---|---:|---:|---|
| 單一老師週複製不包含其他老師 | 通過 | 已做過 | 已自動驗收 |
| 整月複製不包含其他老師 | 通過 | 已做過 | 已自動驗收 |
| 框選、複製、貼上與批次操作不跨老師 | 框選、剪貼簿與批次候選通過 | 已做過 | 部分完成 |
| 單堂拖曳只移該堂 | 通過 | 已做過 | 已自動驗收 |
| 多選拖曳保持相對日期與時間 | 通過 | 已做過 | 已自動驗收 |
| 連續拖曳兩至三堂不被同步阻塞 | 部分覆蓋 | 已做過 | 部分完成 |
| 月曆與週課表立即落位並同步 Firebase | 本機落位與雙客戶端同步通過 | 已做過 | 部分完成 |
| 老師收到更新；校區管理者維持允許範圍 | 待補 | 已做過 | 部分完成 |
| 空白日期不誤判；真正衝突才阻止 | 主要邊界通過 | 已做過 | 部分完成 |

## 執行順序

1. **已完成 P0-01：Firestore Rules Emulator 權限測試**——11 項角色、資料邊界與雙客戶端同步測試通過。
2. **已完成 P2-01：建立 staging 環境**——測試資料與正式 Firebase 完全隔離。
3. **已完成 P2-02：CI 自動檢查**——push、PR 與手動執行皆會驗證程式、情境與 Rules。
4. **已完成 P0-06：錯誤監控最小版本**——只收集版本、環境、錯誤類型與操作區域，不收敏感內容。
5. **已完成 P0-05：備份還原演練**——在獨立 Firestore Emulator 驗證完整還原，未接觸正式資料。
6. **已完成 P0-07：發布與回滾演練**——staging 可在 5 分鐘內回到上一版並恢復最新版。
7. **P1-09 已完成：月結鎖定快照與調整單**——歷史財務不再因日後改課被覆寫。
8. 完成以上項目後才開始家長入口與付款串接。

## 看板更新規則

- 只有「驗收條件全部通過」才能標記為已驗收。
- 人工驗收必須記錄日期、角色、裝置、資料範圍與操作結果。
- 自動測試必須可在乾淨環境重複執行，不依賴正式資料。
- 功能狀態變更時，同一個 commit 內更新本看板與 `CHANGELOG.md`。
- 發現回歸時立即降回「部分完成」，不得保留綠色狀態。
