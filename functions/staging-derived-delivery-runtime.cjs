'use strict';

const {nativeCanonicalSha256}=require('./native-canonical-sha256.cjs');

const PROJECT_ID='danbridge-d8877-staging';
const COMPANY_ID='danbridge';
const PRIMARY_OWNER_EMAIL='a0965487920@gmail.com';

const clone=value=>JSON.parse(JSON.stringify(value));
const text=value=>String(value??'').trim();
const canonicalJsonValue=value=>Array.isArray(value)?value.map(canonicalJsonValue):(value&&typeof value==='object'?Object.fromEntries(Object.keys(value).sort().filter(key=>value[key]!==undefined).map(key=>[key,canonicalJsonValue(value[key])])):value);
const jsonRecordsEqual=(left,right)=>JSON.stringify(canonicalJsonValue(left))===JSON.stringify(canonicalJsonValue(right));
const teacherIds=lesson=>[...new Set((Array.isArray(lesson?.teacherIds)&&lesson.teacherIds.length?lesson.teacherIds:[lesson?.teacherId]).filter(Boolean).map(String))];
const lessonBranchId=lesson=>String(lesson?.branchId||((lesson?.location==='河西一路')?'hexi':(lesson?.location==='到府'||lesson?.location==='線上課')?'unassigned':'art_museum'));
const notificationFields=['date','start','end','studentId','title','location','room','branchId','deliveryMode','address','onlinePlatform','meetingUrl','status','lessonState','note','paymentStatus','chargeStudent','payTeacher','campId'];

function notificationFingerprint(lesson){return notificationFields.map(key=>String(lesson?.[key]??'')).join('|')+'|'+teacherIds(lesson).slice().sort().join(',')}
function lessonTime(lesson){return[lesson?.date,lesson?.start&&lesson?.end?`${lesson.start}–${lesson.end}`:lesson?.start||''].filter(Boolean).join(' ')}
function studentName(lesson,db){return String((db?.students||[]).find(row=>String(row.id)===String(lesson?.studentId))?.name||lesson?.studentName||lesson?.title||'未命名課程')}
function safeLesson(lesson,{teacher=false}={}){if(!lesson)return null;const value={date:lesson.date||'',start:lesson.start||'',end:lesson.end||'',studentId:lesson.studentId||'',title:lesson.title||'',location:lesson.location||'',branchId:lesson.branchId||'',deliveryMode:lesson.deliveryMode||'',room:lesson.room||'',address:lesson.address||'',onlinePlatform:lesson.onlinePlatform||'',meetingUrl:lesson.meetingUrl||'',status:lesson.status||'',note:lesson.note||'',teacherIds:teacherIds(lesson)};if(teacher){value.address='';value.meetingUrl='';value.note=''}return value}
function changeSummary(type,before,after,db){const target=after||before||{},name=studentName(target,db);if(type==='added')return`新增：${name}｜${lessonTime(target)}`;if(type==='removed')return`取消：${name}｜${lessonTime(target)}`;const changes=[];if(String(before?.date||'')!==String(after?.date||''))changes.push(`日期 ${before?.date||'—'} → ${after?.date||'—'}`);if(String(before?.start||'')!==String(after?.start||'')||String(before?.end||'')!==String(after?.end||''))changes.push(`時間 ${(before?.start||'—')}–${(before?.end||'—')} → ${(after?.start||'—')}–${(after?.end||'—')}`);if(String(before?.branchId||'')!==String(after?.branchId||''))changes.push(`校區 ${before?.branchId||'—'} → ${after?.branchId||'—'}`);if(String(before?.room||'')!==String(after?.room||''))changes.push(`教室 ${before?.room||'—'} → ${after?.room||'—'}`);if(String(before?.title||'')!==String(after?.title||''))changes.push(`課程 ${before?.title||'—'} → ${after?.title||'—'}`);if(teacherIds(before).slice().sort().join(',')!==teacherIds(after).slice().sort().join(','))changes.push('授課老師已更新');if(String(before?.status||'')!==String(after?.status||''))changes.push(`狀態 ${before?.status||'—'} → ${after?.status||'—'}`);return`修改：${name}｜${changes.slice(0,3).join('；')||lessonTime(target)}`}

function restorePreviousLessons(currentDb,payload){const previous=clone(currentDb),map=new Map((previous.lessons||[]).map(row=>[String(row.id),row]));for(let index=0;index<(payload.changedKeys||[]).length;index++){const key=payload.changedKeys[index],baseline=payload.baselineRecords?.[index];if(key?.collection!=='lessons'||!baseline||baseline.collection!=='lessons'||baseline.recordId!==key.recordId)continue;if(baseline.exists&&baseline.deleted!==true)map.set(String(key.recordId),clone(baseline.record));else map.delete(String(key.recordId))}previous.lessons=[...map.values()];return previous}
function lessonChanges(previous,current){const before=new Map((previous.lessons||[]).map(row=>[String(row.id),row])),after=new Map((current.lessons||[]).map(row=>[String(row.id),row])),rows=[];for(const[id,value]of after){const prior=before.get(id);if(!prior)rows.push({type:'added',lessonId:id,before:null,after:value});else if(notificationFingerprint(prior)!==notificationFingerprint(value))rows.push({type:'modified',lessonId:id,before:prior,after:value})}for(const[id,value]of before)if(!after.has(id))rows.push({type:'removed',lessonId:id,before:value,after:null});return rows}

function applyPayloadToCachedDb(previousDb,payload){
 const keys=payload?.changedKeys||[],baselines=payload?.baselineRecords||[],locals=payload?.localRecords||[],touched=new Set(keys.map(key=>text(key?.collection))),next={...previousDb};
 if(!keys.length||keys.length!==baselines.length||keys.length!==locals.length)throw new Error('staging derived cache payload count invalid');
 for(const collection of touched){if(!Array.isArray(previousDb?.[collection]))throw new Error(`staging derived cache collection invalid: ${collection}`);next[collection]=[...previousDb[collection]]}
 for(let index=0;index<keys.length;index++){
  const key=keys[index],baseline=baselines[index],local=locals[index],collection=text(key?.collection),recordId=text(key?.recordId);
  if(!Object.prototype.hasOwnProperty.call(next,collection)||!Array.isArray(next[collection])||!recordId||baseline?.collection!==collection||baseline?.recordId!==recordId||local?.collection!==collection||local?.recordId!==recordId)throw new Error('staging derived cache payload identity invalid');
  const rows=next[collection],position=rows.findIndex(row=>String(row?.id)===recordId),current=position>=0?rows[position]:null,baselinePresent=baseline.exists===true&&baseline.deleted!==true;
  if(baselinePresent?(!current||!jsonRecordsEqual(current,baseline.record)):(current!==null))throw new Error(`staging derived cache baseline mismatch: ${collection}/${recordId}`);
  const localPresent=local.exists===true&&local.deleted!==true;
  if(localPresent){const value=clone(local.record);if(!value||typeof value!=='object'||Array.isArray(value)||String(value.id)!==recordId)throw new Error(`staging derived cache local record invalid: ${collection}/${recordId}`);if(position>=0)rows[position]=value;else rows.push(value)}else if(position>=0)rows.splice(position,1);
 }
 return next
}

function applyAuditPayloadToCachedDb(previousDb,payload,{buildRecordId}={}){
 const next={...previousDb,changes:[...(previousDb?.changes||[])]},keys=payload?.changedKeys||[],baselines=payload?.baselineRecords||[],locals=payload?.localRecords||[];
 if(typeof buildRecordId!=='function'||keys.length!==1||baselines.length!==1||locals.length!==1)throw new Error('staging derived audit cache payload count invalid');
 const key=keys[0],baseline=baselines[0],local=locals[0],recordId=text(key?.recordId),rows=next?.changes;
 if(!Array.isArray(rows)||key?.collection!=='changes'||!recordId||baseline?.collection!=='changes'||baseline?.recordId!==recordId||baseline.exists!==false||baseline.deleted!==false||baseline.record!==null||local?.collection!=='changes'||local?.recordId!==recordId||local.exists!==true||local.deleted!==false||!local.record||typeof local.record!=='object'||Array.isArray(local.record))throw new Error('staging derived audit cache payload identity invalid');
 const existingPosition=rows.findIndex((row,index)=>buildRecordId(rows.length-1-index,row)===recordId);
 if(existingPosition>=0){if(!jsonRecordsEqual(rows[existingPosition],local.record))throw new Error(`staging derived audit cache replay mismatch: ${recordId}`);return next}
 const recordIndex=rows.length;
 if(buildRecordId(recordIndex,local.record)!==recordId)throw new Error(`staging derived audit cache append sequence invalid: ${recordId}`);
 rows.unshift(clone(local.record));
 return next
}

function applyAuditPayloadsToCachedDb(previousDb,payload,{buildRecordId}={}){
 const next={...previousDb,changes:[...(previousDb?.changes||[])]},keys=payload?.changedKeys||[],baselines=payload?.baselineRecords||[],locals=payload?.localRecords||[];
 if(typeof buildRecordId!=='function'||!keys.length||keys.length!==baselines.length||keys.length!==locals.length)throw new Error('staging derived audit batch cache payload count invalid');
 const rows=next.changes;
 for(let index=0;index<keys.length;index++){
  const key=keys[index],baseline=baselines[index],local=locals[index],recordId=text(key?.recordId);
  if(key?.collection!=='changes'||!recordId||baseline?.collection!=='changes'||baseline?.recordId!==recordId||baseline.exists!==false||baseline.deleted!==false||baseline.record!==null||local?.collection!=='changes'||local?.recordId!==recordId||local.exists!==true||local.deleted!==false||!local.record||typeof local.record!=='object'||Array.isArray(local.record))throw new Error('staging derived audit batch cache payload identity invalid');
  const existingPosition=rows.findIndex((row,rowIndex)=>buildRecordId(rows.length-1-rowIndex,row)===recordId);
  if(existingPosition>=0){if(!jsonRecordsEqual(rows[existingPosition],local.record))throw new Error(`staging derived audit batch cache replay mismatch: ${recordId}`);continue}
  const recordIndex=rows.length;
  if(buildRecordId(recordIndex,local.record)!==recordId)throw new Error(`staging derived audit batch cache append sequence invalid: ${recordId}`);
  rows.unshift(clone(local.record));
 }
 return next
}

function selectPayload(payload,predicate){const indexes=[];for(let index=0;index<(payload?.changedKeys||[]).length;index++)if(predicate(payload.changedKeys[index],index))indexes.push(index);return{save:payload.save,changedKeys:indexes.map(index=>payload.changedKeys[index]),baselineRecords:indexes.map(index=>payload.baselineRecords[index]),localRecords:indexes.map(index=>payload.localRecords[index])}}
function applyAuthorityPayloadToCachedDb(previousDb,payload,{buildRecordId}={}){let next={...previousDb};const records=selectPayload(payload,key=>key?.collection!=='changes');if(records.changedKeys.length)next=applyPayloadToCachedDb(next,records);const audits=selectPayload(payload,key=>key?.collection==='changes');if(audits.changedKeys.length)next=applyAuditPayloadsToCachedDb(next,audits,{buildRecordId});return next}

function applyPayloadToCachedDocuments(previous,payload,sourceHash){
 const keys=payload?.changedKeys||[],baselines=payload?.baselineRecords||[],locals=payload?.localRecords||[],touched=new Set(keys.map(key=>text(key?.collection))),next={...previous};
 if(!previous||typeof previous!=='object'||Array.isArray(previous)||!/^record-v1:[a-f0-9]{64}$/.test(sourceHash)||!keys.length||keys.length!==baselines.length||keys.length!==locals.length)throw new Error('staging derived document cache payload invalid');
 for(const collection of touched){if(!Array.isArray(previous?.[collection]))throw new Error(`staging derived document cache collection invalid: ${collection}`);next[collection]=[...previous[collection]]}
 for(let index=0;index<keys.length;index++){
  const key=keys[index],baseline=baselines[index],local=locals[index],collection=text(key?.collection),recordId=text(key?.recordId),rows=next[collection];
  if(!Array.isArray(rows)||!recordId||baseline?.collection!==collection||baseline?.recordId!==recordId||local?.collection!==collection||local?.recordId!==recordId||local.exists!==true||!Number.isSafeInteger(baseline.revision)||baseline.revision<0||local.revision!==baseline.revision)throw new Error('staging derived document cache identity invalid');
  const position=rows.findIndex(row=>String(row?.id)===recordId),revision=baseline.revision+1,recordIndex=collection==='changes'?Number(/^seq_(\d{8,})_/.exec(recordId)?.[1]):null,data={schema:'danbridge-full-record-shadow-v1',companyId:COMPANY_ID,collection,recordId,record:clone(local.record),recordIndex,sourceHash,revision,deleted:local.deleted===true,environment:'staging'};
  if(collection==='changes'&&!Number.isSafeInteger(recordIndex))throw new Error('staging derived document cache audit index invalid');
  if(position>=0){const current=rows[position]?.data;if(Number(current?.revision)===revision&&current?.deleted===data.deleted&&jsonRecordsEqual(current?.record,data.record)){rows[position]={id:recordId,data:{...current,sourceHash}};continue}rows[position]={id:recordId,data}}
  else rows.push({id:recordId,data});
 }
 return next
}

function advanceCachedSourceModel(previous,payload,sourceDb,sourceHash){
 if(!previous||!sourceDb||typeof sourceDb!=='object'||!Number.isSafeInteger(previous.documentCount)||!Number.isSafeInteger(previous.activeCount)||!Number.isSafeInteger(previous.tombstoneCount)||!previous.revisions||!/^record-v1:[a-f0-9]{64}$/.test(sourceHash))throw new Error('staging derived source model invalid');
 const revisions={...previous.revisions},touched=new Set(),keys=payload?.changedKeys||[],baselines=payload?.baselineRecords||[],locals=payload?.localRecords||[];let documentCount=previous.documentCount,activeCount=previous.activeCount,tombstoneCount=previous.tombstoneCount;
 if(!keys.length||keys.length!==baselines.length||keys.length!==locals.length)throw new Error('staging derived source model payload invalid');
 for(let index=0;index<keys.length;index++){
  const key=keys[index],baseline=baselines[index],local=locals[index],collection=text(key?.collection),recordId=text(key?.recordId);
  if(!collection||!recordId||baseline?.collection!==collection||baseline?.recordId!==recordId||local?.collection!==collection||local?.recordId!==recordId||local.exists!==true||!Number.isSafeInteger(baseline.revision)||baseline.revision<0||local.revision!==baseline.revision)throw new Error('staging derived source model identity invalid');
  if(!touched.has(collection)){if(!previous.revisions[collection]||typeof previous.revisions[collection]!=='object')throw new Error('staging derived source model revisions invalid');revisions[collection]={...previous.revisions[collection]};touched.add(collection)}
  const beforeDocument=baseline.exists===true,beforeActive=beforeDocument&&baseline.deleted!==true,afterActive=local.deleted!==true;
  if(!beforeDocument){documentCount++;if(afterActive)activeCount++;else tombstoneCount++}
  else if(beforeActive!==afterActive){activeCount+=afterActive?1:-1;tombstoneCount+=afterActive?-1:1}
  revisions[collection][recordId]=baseline.revision+1;
 }
 if(documentCount<0||activeCount<0||tombstoneCount<0||documentCount!==activeCount+tombstoneCount)throw new Error('staging derived source model count invariant invalid');
 return{db:sourceDb,hash:sourceHash,documentCount,activeCount,tombstoneCount,revisions}
}

function recipientGroups(accessRows,changes){const grouped=new Map(),add=(recipient,change,teacherId='')=>{if(!grouped.has(recipient.email))grouped.set(recipient.email,{recipient,teacherId,changes:new Map()});grouped.get(recipient.email).changes.set(`${change.type}:${change.lessonId}`,change)},owners=[{email:PRIMARY_OWNER_EMAIL,role:'owner',teacherName:'Daniel',branchIds:[]}],teachers=new Map(),schedulers=[],managers=[];for(const access of accessRows){const email=text(access.email||access.id).toLowerCase();if(access.active!==true||access.companyId!==COMPANY_ID||!email)continue;if(access.role==='owner'){if(!owners.some(row=>row.email===email))owners.push({email,role:'owner',teacherName:access.displayName||'',branchIds:[]})}else if(access.role==='branch_manager'&&Array.isArray(access.branchIds)&&access.branchIds.length)managers.push({email,role:'branch_manager',teacherName:access.managerName||access.teacherName||'',branchIds:access.branchIds.map(String)});else if(access.role==='teacher'){if(access.canManageSchedule===true)schedulers.push({email,role:'scheduler',teacherName:access.teacherName||access.displayName||'aa',branchIds:[]});if(access.teacherId){const id=String(access.teacherId);if(!teachers.has(id))teachers.set(id,[]);teachers.get(id).push({email,role:'teacher',teacherName:access.teacherName||access.displayName||'',branchIds:[]})}}}
 for(const change of changes){for(const owner of owners)add(owner,change);for(const scheduler of schedulers)add(scheduler,change);const affectedTeachers=new Set([...teacherIds(change.before),...teacherIds(change.after)]);for(const id of affectedTeachers)for(const teacher of teachers.get(id)||[])add(teacher,change,id);const branches=new Set([change.before&&lessonBranchId(change.before),change.after&&lessonBranchId(change.after)].filter(Boolean));for(const manager of managers)if(manager.branchIds.some(id=>branches.has(id)))add(manager,change)}return[...grouped.values()]}

function buildNotifications({payload,currentDb,accessRows,sourceHash,hash}){if(!(payload.changedKeys||[]).some(key=>key.collection==='lessons'))return[];const previous=restorePreviousLessons(currentDb,payload),changes=lessonChanges(previous,currentDb);if(!changes.length)return[];return recipientGroups(accessRows,changes).map(({recipient,teacherId,changes:itemsMap})=>{const items=[...itemsMap.values()],isTeacher=recipient.role==='teacher',details=items.map(item=>({type:item.type,lessonId:item.lessonId,summary:changeSummary(item.type,item.before,item.after,currentDb),studentName:studentName(item.after||item.before,currentDb),beforeTime:item.before?lessonTime(item.before):'',afterTime:item.after?lessonTime(item.after):'',before:safeLesson(item.before,{teacher:isTeacher}),after:safeLesson(item.after,{teacher:isTeacher})})),id=`sv2_${hash({saveId:payload.save.saveId,email:recipient.email}).slice(0,48)}`;return{id,payload:{companyId:COMPANY_ID,recipientEmail:recipient.email,recipientRole:recipient.role,teacherId,branchIds:recipient.role==='branch_manager'?recipient.branchIds:[],teacherName:recipient.teacherName||'',title:'課表更新通知',message:recipient.role==='owner'?`公司課表有 ${items.length} 個變更`:recipient.role==='scheduler'?`全老師課表有 ${items.length} 個變更`:recipient.role==='branch_manager'?`您管理的校區課表有 ${items.length} 個變更`:`您的課表有 ${items.length} 個變更`,changeCount:details.length,details,read:false,createdBy:payload.save.actorUid,createdByName:payload.save.actorEmail,sourceRecordHash:sourceHash,sourceSaveId:payload.save.saveId}}})}

async function createStagingDerivedDeliveryRuntime({firestore,serverTimestamp,now=()=>Date.now(),expectedProjectId=PROJECT_ID}={}){
 if(expectedProjectId!==PROJECT_ID||!firestore||typeof firestore.doc!=='function'||typeof firestore.collection!=='function'||typeof firestore.batch!=='function'||typeof firestore.runTransaction!=='function'||typeof serverTimestamp!=='function'||typeof now!=='function')throw new Error('staging derived delivery boundary invalid');
 const [{FULL_RECORD_COLLECTIONS,rebuildFullRecordShadowDb},{normalizeRecordDb},{sha256Canonical},{createStagingV2AuthorityReadLoader},{createFirebaseRoleRecordViewAdapter},{roleRecordViewKey},projection,{buildChangeRecordId}]=await Promise.all([import('../js/core/cloud-full-record-shadow.js'),import('../js/core/cloud-record-data-hash.js'),import('../js/core/cloud-immutable-migration-backup.js'),import('../js/core/staging-v2-authority-read-loader.js'),import('../js/core/firebase-role-record-view-adapter.js'),import('../js/core/cloud-role-record-view.js'),import('../js/core/production-role-view-projection.js'),import('../js/core/cloud-change-record-identity.js')]);
 const recordDataHash=db=>`record-v1:${nativeCanonicalSha256(normalizeRecordDb(db))}`;
 const readDocument=async path=>{const snapshot=await firestore.doc(path).get();return snapshot.exists?snapshot.data():null},readCollection=async path=>(await firestore.collection(path).get()).docs.map(row=>({id:row.id,data:row.data()})),runTransaction=callback=>firestore.runTransaction(native=>callback({get:path=>native.get(firestore.doc(path)),getAll:(...paths)=>native.getAll(...paths.map(path=>firestore.doc(path))),set:(path,value,options={merge:false})=>native.set(firestore.doc(path),value,options)}));
 const loader=createStagingV2AuthorityReadLoader({expectedProjectId,getDocumentFromServer:readDocument,getCollectionFromServer:readCollection});
 let cache=null,warmPromise=null;
 const headPath=activationEpoch=>`stagingActiveRecordV2Heads/${COMPANY_ID}/epochs/${activationEpoch}`;
 const auditCursorPath=activationEpoch=>`stagingActiveRecordV2AuditCursors/${COMPANY_ID}/epochs/${activationEpoch}`;
 const hydrate=async activationEpoch=>{const [model,auditCursor]=await Promise.all([loader.load({activationEpoch}),readDocument(auditCursorPath(activationEpoch))]),rebuilt=rebuildFullRecordShadowDb(model.documentsByCollection,{environment:'staging'}),sourceDb=rebuilt.db,sourceHash=recordDataHash(sourceDb),sourceModel={...rebuilt,db:sourceDb,hash:sourceHash};cache={activationEpoch,headHash:model.headHash,sourceHash,sourceDb,sourceModel,documentsByCollection:model.documentsByCollection,auditRevision:Number(auditCursor?.revision)||0,auditLastRecordId:text(auditCursor?.lastRecordId)};return{model,sourceDb,sourceHash,sourceModel}};
 const warm=()=>{if(warmPromise===null)warmPromise=(async()=>{const fence=await readDocument('stagingRecordSyncV1PermanentFences/danbridge'),activationEpoch=text(fence?.targetV2Epoch);if(!activationEpoch)throw new Error('staging derived warmup activation epoch missing');const started=now(),value=await hydrate(activationEpoch);console.info('STAGING_V2_DERIVED_WARM',JSON.stringify({state:'verified',activationEpoch,sourceHash:value.sourceHash,totalMs:now()-started}));return{state:'verified',activationEpoch,sourceHash:value.sourceHash}})().catch(error=>{warmPromise=null;throw error});return warmPromise};
  async function publishRoleViews(sourceDb,previousDb,accessRows,activationEpoch,sourceHash,previousSourceHash,save,{incremental=false}={}){const projectedAt=now(),views=projection.buildProductionRoleViews(sourceDb,accessRows,{now:projectedAt}),previousViews=projection.buildProductionRoleViews(previousDb,accessRows,{now:projectedAt}),previousByKey=new Map(previousViews.map(view=>[roleRecordViewKey({email:view.email,kind:view.kind,teacherId:view.teacherId,branchIds:view.branchIds||[]},activationEpoch),view])),keys=views.map(view=>roleRecordViewKey({email:view.email,kind:view.kind,teacherId:view.teacherId,branchIds:view.branchIds||[]},activationEpoch)),readDocuments=async paths=>(await firestore.getAll(...paths.map(path=>firestore.doc(path)))).map(snapshot=>snapshot.exists?snapshot.data():null);if(new Set(keys).size!==keys.length)throw new Error('staging role view projection contains duplicate identities');return Promise.all(views.map(async(view,index)=>{const identity={email:view.email,kind:view.kind,teacherId:view.teacherId,branchIds:view.branchIds||[]},adapter=createFirebaseRoleRecordViewAdapter({environment:'staging',role:'owner',actor:{uid:save.actorUid,email:save.actorEmail},getDocument:readDocument,getCollectionDocuments:readCollection,runBatchTransaction:runTransaction,runTransaction,serverTimestamp,singleReadback:true}),options={identity,activationEpoch,sourceRecordHash:sourceHash,publishId:`srv_${String(save.saveId).slice(0,92)}_${String(index).padStart(2,'0')}`,publishedAt:new Date(now()).toISOString(),batchSize:100},previous=previousByKey.get(keys[index]);if(incremental&&previous){try{return await adapter.synchronizeIncremental(previous.db,view.db,{...options,previousSourceRecordHash:previousSourceHash,readDocuments})}catch(error){console.warn('STAGING_ROLE_VIEW_INCREMENTAL_FALLBACK',JSON.stringify({email:view.email,message:String(error?.message||'fallback')}))}}return adapter.synchronize(view.db,options)}))}
 async function publishLessonMeta(sourceDb,{changedLessonIds=[],incremental=false}={}){const path=`companies/${COMPANY_ID}/lessonMeta`,desired=projection.buildProductionLessonMeta(sourceDb),desiredById=new Map(desired.map(row=>[row.lessonId,row]));if(incremental&&changedLessonIds.length){const ids=[...new Set(changedLessonIds.map(String))],refs=ids.map(id=>firestore.doc(`${path}/${id}`)),current=await firestore.getAll(...refs),writes=[];for(let index=0;index<ids.length;index++){const id=ids[index],row=desiredById.get(id),saved=current[index].exists?current[index].data():null;if(row){if(!saved||projection.productionLessonMetaSignature(saved)!==projection.productionLessonMetaSignature(row.payload))writes.push({type:'set',ref:refs[index],value:{...row.payload,updatedAt:serverTimestamp()}})}else if(saved)writes.push({type:'delete',ref:refs[index]})}if(writes.length){const batch=firestore.batch();for(const write of writes)write.type==='set'?batch.set(write.ref,write.value,{merge:false}):batch.delete(write.ref);await batch.commit()}const verified=await firestore.getAll(...refs);for(let index=0;index<ids.length;index++){const row=desiredById.get(ids[index]),saved=verified[index].exists?verified[index].data():null;if(row?(!saved||projection.productionLessonMetaSignature(saved)!==projection.productionLessonMetaSignature(row.payload)):Boolean(saved))throw new Error(`staging lessonMeta incremental readback mismatch: ${ids[index]}`)}return writes.length}const current=(await firestore.collection(path).get()).docs,byId=new Map(current.map(row=>[row.id,row.data()])),desiredIds=new Set(desired.map(row=>row.lessonId)),writes=[];for(const row of desired){if(!byId.has(row.lessonId)||projection.productionLessonMetaSignature(byId.get(row.lessonId))!==projection.productionLessonMetaSignature(row.payload))writes.push({type:'set',ref:firestore.doc(`${path}/${row.lessonId}`),value:{...row.payload,updatedAt:serverTimestamp()}})}for(const row of current)if(!desiredIds.has(row.id))writes.push({type:'delete',ref:row.ref});for(let offset=0;offset<writes.length;offset+=400){const batch=firestore.batch();for(const write of writes.slice(offset,offset+400))write.type==='set'?batch.set(write.ref,write.value,{merge:false}):batch.delete(write.ref);await batch.commit()}const verified=(await firestore.collection(path).get()).docs;if(verified.length!==desired.length)throw new Error('staging lessonMeta readback count mismatch');const verifiedMap=new Map(verified.map(row=>[row.id,row.data()]));for(const row of desired)if(projection.productionLessonMetaSignature(verifiedMap.get(row.lessonId))!==projection.productionLessonMetaSignature(row.payload))throw new Error(`staging lessonMeta readback mismatch: ${row.lessonId}`);return writes.length}
 async function publishNotifications(payload,sourceDb,accessRows,sourceHash){
  const rows=buildNotifications({payload,currentDb:sourceDb,accessRows,sourceHash,hash:sha256Canonical});
  if(!rows.length)return 0;
  const refs=rows.map(row=>firestore.doc(`companies/${COMPANY_ID}/scheduleNotifications/${row.id}`));
  // Every recipient belongs to the same immutable schedule event. Commit them
  // together so Daniel, AA and the affected teachers cannot observe a partial
  // notification fan-out, and avoid one network transaction per recipient.
  await firestore.runTransaction(async transaction=>{
   const current=await transaction.getAll(...refs);
   for(let index=0;index<rows.length;index++){
    const snapshot=current[index],data=snapshot.exists?snapshot.data():null,row=rows[index];
    if(data){
     if(data.sourceRecordHash!==sourceHash||data.sourceSaveId!==payload.save.saveId||data.recipientEmail!==row.payload.recipientEmail||data.changeCount!==row.payload.changeCount)throw new Error(`staging notification immutable identity mismatch: ${row.id}`);
     continue
    }
    transaction.set(refs[index],{...row.payload,createdAt:serverTimestamp()},{merge:false})
   }
  });
  const verified=await firestore.getAll(...refs);
  for(let index=0;index<rows.length;index++){const data=verified[index].exists?verified[index].data():null;if(!data||data.sourceRecordHash!==sourceHash||data.sourceSaveId!==payload.save.saveId||data.recipientEmail!==rows[index].payload.recipientEmail||data.changeCount!==rows[index].payload.changeCount)throw new Error(`staging notification readback mismatch: ${rows[index].id}`)}
  return rows.length
 }
 const snapshot=()=>cache?clone(cache):null,peek=()=>cache;
 return Object.freeze({warm,snapshot,peek,async deliver(payload,completion,trustedHashes=null){
  if(!payload||!completion||completion.projectId!==PROJECT_ID||completion.activationEpoch!==text(completion.activationEpoch)||completion.saveId!==payload.save?.saveId)throw new Error('staging derived delivery request identity invalid');
  const started=now(),activationEpoch=completion.activationEpoch,liveHead=await readDocument(headPath(activationEpoch)),auditOnly=payload.changedKeys?.length>0&&payload.changedKeys.every(key=>key?.collection==='changes');
  if(auditOnly){
   if(!liveHead||typeof liveHead.headHash!=='string'||!/^[a-f0-9]{64}$/.test(liveHead.headHash)||typeof completion.resultHeadHash!=='string'||!/^[a-f0-9]{64}$/.test(completion.resultHeadHash))throw new Error('staging derived audit delivery hash identity invalid');
   const cacheForLiveHead=cache?.activationEpoch===activationEpoch&&cache.headHash===liveHead.headHash;let sourceDb,cacheHit=false;
   if(cacheForLiveHead){cacheHit=true;sourceDb=applyAuthorityPayloadToCachedDb(cache.sourceDb,payload,{buildRecordId:buildChangeRecordId})}
   else{const loaded=await hydrate(activationEpoch);if(loaded.model.headHash!==liveHead.headHash)throw new Error('staging derived audit delivery loaded head mismatch');sourceDb=loaded.sourceDb}
   const sourceHash=recordDataHash(sourceDb),documentsByCollection=cacheHit?applyPayloadToCachedDocuments(cache.documentsByCollection,payload,sourceHash):cache.documentsByCollection,sourceModel=cacheHit?advanceCachedSourceModel(cache.sourceModel,payload,sourceDb,sourceHash):cache.sourceModel;cache={...cache,activationEpoch,headHash:liveHead.headHash,sourceHash,sourceDb,sourceModel,documentsByCollection,auditRevision:cacheHit?(Number(cache.auditRevision)||0)+payload.changedKeys.length:Number(cache.auditRevision)||0,auditLastRecordId:cacheHit?text(payload.changedKeys.at(-1).recordId):text(cache.auditLastRecordId)};
   return Object.freeze({state:'verified',skippedAuditAppend:true,cacheHit,sourceHash,totalMs:now()-started})
  }
  if(liveHead?.headHash!==completion.resultHeadHash)throw new Error('staging derived delivery head changed before publication');
  const cacheCurrent=cache?.activationEpoch===activationEpoch&&cache.headHash===completion.resultHeadHash,cachePrevious=cache?.activationEpoch===activationEpoch&&cache.headHash===liveHead.previousHeadHash,auditKeys=(payload.changedKeys||[]).filter(key=>key.collection==='changes'),recordPayload=selectPayload(payload,key=>key?.collection!=='changes'),trustedSourceHash=text(trustedHashes?.sourceHash),trustedPreviousSourceHash=text(trustedHashes?.previousSourceHash),trustedHashPair=/^record-v1:[a-f0-9]{64}$/.test(trustedSourceHash)&&/^record-v1:[a-f0-9]{64}$/.test(trustedPreviousSourceHash);
  const lessonOnly=recordPayload.changedKeys.length>0&&recordPayload.changedKeys.every(key=>key.collection==='lessons');let sourceDb,sourceHash,previousDb,cacheHit=false,loadedCurrent=false;
  if(lessonOnly&&(cacheCurrent||cachePrevious)){
   cacheHit=true;if(cacheCurrent){sourceDb=cache.sourceDb;previousDb=restorePreviousLessons(sourceDb,recordPayload)}else{previousDb=cache.sourceDb;sourceDb=applyAuthorityPayloadToCachedDb(previousDb,payload,{buildRecordId:buildChangeRecordId})}sourceHash=trustedHashPair&&trustedSourceHash!==trustedPreviousSourceHash?trustedSourceHash:recordDataHash(sourceDb)
  }else{const loaded=await hydrate(activationEpoch);loadedCurrent=true;if(loaded.model.headHash!==completion.resultHeadHash)throw new Error('staging derived delivery loaded head mismatch');sourceDb=loaded.sourceDb;sourceHash=loaded.sourceHash;previousDb=lessonOnly?restorePreviousLessons(sourceDb,recordPayload):sourceDb}
  const previousSourceHash=lessonOnly&&(trustedHashPair&&trustedPreviousSourceHash===(cachePrevious?cache.sourceHash:sourceHash))?trustedPreviousSourceHash:(lessonOnly?recordDataHash(previousDb):sourceHash);if(trustedHashPair&&(sourceHash!==trustedSourceHash||previousSourceHash!==trustedPreviousSourceHash))throw new Error('staging derived trusted hash pair mismatch');const changedLessonIds=lessonOnly?recordPayload.changedKeys.map(key=>key.recordId):[],access=(await firestore.collection('companyAccess').get()).docs.map(row=>({id:row.id,...(row.data()||{})})),roleStarted=now(),rolePromise=publishRoleViews(sourceDb,previousDb,access,activationEpoch,sourceHash,previousSourceHash,payload.save,{incremental:lessonOnly}).then(value=>({value,ms:now()-roleStarted})),metaStarted=now(),metaPromise=publishLessonMeta(sourceDb,{changedLessonIds,incremental:lessonOnly}).then(value=>({value,ms:now()-metaStarted})),notificationStarted=now(),notificationPromise=publishNotifications(payload,sourceDb,access,sourceHash).then(value=>({value,ms:now()-notificationStarted})),[roleResult,metaResult,notificationResult]=await Promise.all([rolePromise,metaPromise,notificationPromise]),headAfter=await readDocument(headPath(activationEpoch));
  if(headAfter?.headHash!==completion.resultHeadHash)throw new Error('staging derived delivery head changed during publication');const advancing=cachePrevious&&!cacheCurrent&&!loadedCurrent,documentsByCollection=(cacheCurrent||loadedCurrent)?cache.documentsByCollection:applyPayloadToCachedDocuments(cache.documentsByCollection,payload,sourceHash),auditRevision=advancing?(Number(cache.auditRevision)||0)+auditKeys.length:Number(cache.auditRevision)||0,auditLastRecordId=advancing&&auditKeys.length?text(auditKeys.at(-1).recordId):text(cache.auditLastRecordId),sourceModel=advancing?advanceCachedSourceModel(cache.sourceModel,payload,sourceDb,sourceHash):cache.sourceModel;cache={...cache,activationEpoch,headHash:completion.resultHeadHash,sourceHash,sourceDb,sourceModel,documentsByCollection,auditRevision,auditLastRecordId};const roleViews=roleResult.value,result=Object.freeze({state:'verified',sourceHash,cacheHit,roleViewCount:roleViews.length,incrementalRoleViews:roleViews.filter(row=>row.incremental).length,lessonMetaWrites:metaResult.value,notificationCount:notificationResult.value,roleMs:roleResult.ms,lessonMetaMs:metaResult.ms,notificationMs:notificationResult.ms,totalMs:now()-started});console.info('STAGING_V2_DERIVED_DELIVERY',JSON.stringify(result));return result
 }})
}

module.exports={createStagingDerivedDeliveryRuntime,restorePreviousLessons,lessonChanges,buildNotifications,applyPayloadToCachedDb,applyAuditPayloadToCachedDb,applyAuditPayloadsToCachedDb,applyAuthorityPayloadToCachedDb,applyPayloadToCachedDocuments,advanceCachedSourceModel};
